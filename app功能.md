# XLine 划线小车 App 功能

## 1. 项目定位

本项目是与运行 `xline_cyg` ROS2 工作空间的 XLine 划线小车配套使用的 Flutter App。

系统由三部分组成：

- Flutter App：负责设备管理、实时状态、人工控制、地图、图纸、任务和 AI 助手界面。
- FastAPI Agent Gateway：负责账号外的设备连接、状态聚合、AI 工具调用、安全门禁、任务管理和审计。
- `xline_cyg` ROS2：负责车轮驱动、IMU、定位、路径规划、路径跟踪、速度仲裁和喷码机控制。

App 不直接操作 CAN 电机，而是通过 FastAPI 后端发送 ROS2 指令。手动速度进入 `/tablet_cmd_vel`，再由 `cmd_vel_mux` 输出最终 `/cmd_vel`。

## 2. 设备管理

- 添加真实小车设备。
- 配置设备名称、IP、FastAPI 端口和 ROS Domain ID。
- 保存最近使用的设备，重新打开 App 后自动恢复。
- 支持保存多台小车配置。
- 切换当前小车。
- 删除不再使用的设备。
- 手动连接、重新连接和断开连接。
- 显示 WebSocket 连接状态。
- 显示后端、ROS2、CAN、电机、定位和任务节点状态。
- 断开连接后清除旧的运行状态，并隐藏不可用的急停操作。

默认实车连接示例：

```text
ws://192.168.0.100:8000
http://192.168.0.100:8000
```

## 3. 首页与状态总览

- 显示当前设备和连接地址。
- 显示已连接、连接中、连接失败和未连接状态。
- 显示底盘是否就绪。
- 显示当前线速度。
- 显示定位是否有效以及定位来源。
- 显示喷码机节点和连接状态。
- 喷码机节点存在不代表喷头已连接；只有实际喷头上报在线/连接后，后端才将喷码能力标记为可用。
- 显示当前任务阶段和完成进度。
- 显示实时地图、车辆位置、航向和轨迹。
- 提供地图、手动控制和设备管理快捷入口。
- 提供全局软件急停按钮。
- 提供可拖动的 AI 助手入口。

## 4. 手动控制

手动控制页面只保留底盘遥控功能，喷码机控制已经移动到设置页面。

- 圆形虚拟摇杆控制前进、后退、左转、右转和组合运动。
- 实时显示摇杆目标线速度和角速度。
- 提供前进、后退、左转、右转和停止按钮。
- 按住方向按钮持续发送速度，松手立即发送零速度。
- 摇杆松手、触摸取消、页面关闭或 App 进入后台时发送零速度。
- 摇杆拖动期间锁定页面滚动，防止页面跟随手指移动。
- 控制指令以约 50 ms 周期持续发送。
- 摇杆死区采用连续缩放，减少起步速度突变。
- 提供慢速、标准、快速三个预设档位。
- 支持设置最大线速度和最大角速度。
- App 侧速度受小车最大速度限制约束。
- 当前实车方向适配在 ROS2 边界统一处理。

手动控制必须同时满足：

- App 已连接后端。
- App 已取得控制权。
- 软件急停已解除。
- CAN 接口已连接。
- 电机驱动已就绪。

## 5. 急停与控制权

- App 连接后自动申请小车控制权。
- 使用控制租约和周期心跳防止多个客户端同时控制。
- 解除急停前自动确认控制权。
- 未取得控制权时禁止解除急停和发送非零速度。
- 急停按钮可随时发送零速度并锁定运动。
- WebSocket 断开、控制租约过期、状态超时或后端关闭时执行安全停车。
- AI 运动和手动控制发生冲突时，手动控制可以接管并停止 AI 运动。
- ROS2 context 已失效时，后端会记录发布失败，不会因二次停车异常而崩溃。
- 硬件急停仍是最高优先级安全装置，App 不能替代硬件急停。

## 6. 地图与定位

### 6.1 有全站仪模式

- 使用 LN150 与 IMU 提供绝对定位。
- 显示绝对位置、航向、规划路径和车辆轨迹。
- LN150 节点和服务存在时提供初始化、自动追踪和自动调平操作。
- ROS2 提供定位校准服务时支持定位校准；服务不存在时按钮保持禁用。

### 6.2 无全站仪模式

- 使用车轮里程计和 IMU 的相对定位。
- 启动位置作为相对原点。
- 通过 `/odom` 与 `/imu` 生成 `/robot_pose`。
- 根据 `/localization/valid` 判断定位数据是否新鲜有效。
- 地图显示相对位置、航向、规划路径和行驶轨迹。
- 不显示 LN150 专用按钮。
- 只有 ROS2 存在 `/localization/calibrate_pose` 服务时才允许重置相对原点；没有服务时显示禁用状态。

### 6.3 地图数据

- 显示 `/robot_pose` 车辆位姿。
- 显示 `/odom` 里程计数据。
- 显示规划路径和路径类型标记。
- 显示实际行驶轨迹 `pose_trace`。
- 显示地图数据和路径数据的新鲜度。
- 支持展开地图查看。
- 定位无效时禁止启动依赖定位的规划路径任务。

## 7. 图纸与路径编辑

- 新建划线图纸。
- 导入 JSON 图纸。
- 支持线段、折线、圆、圆弧、椭圆、样条曲线和文字等几何对象。
- 精确输入坐标、尺寸、角度和半径。
- 双指始终用于平移、缩放和旋转画布，不需要切换单独的视图工具。
- 支持网格与吸附。
- 选择工具支持单指命中路径顶点或线段中部，并拖动整个图形；吸附按总位移计算，避免被图形自身锁住。
- 支持撤销和重做。
- 支持删除对象。
- 支持喷墨路径与转场路径分层。
- 保存前执行图纸检查。
- 检查无效几何、重复 ID、过短线段、小半径转向和不可预览对象。
- JSON 图纸使用毫米作为 CAD 坐标单位。
- 保存后生成路径预览。
- 支持图纸版本管理和版本记录。
- 过滤重复喷墨路径，降低重复喷墨风险。

## 8. 任务规划与执行

- 选择已有 JSON 图纸。
- 查看图纸和路径预览。
- 调用 `/plan_path` 进行路径规划。
- 读取规划结果并显示规划路径。
- 规划确认后调用 `/execute_plan` 执行路径。
- 支持开始、暂停、继续和取消任务。
- 暂停或取消时发送零速度。
- 区分喷墨路径和小车转场路径。
- 跟踪当前任务文件、任务阶段、当前分段、总分段和已完成数量。
- 支持任务前检查和任务摘要。
- 支持规划质量评分。
- 支持喷墨任务账本。
- 支持分段检查点和分段执行验证。
- 支持摆头/航向振荡检测。
- 支持异常恢复建议。
- 支持任务验收报告。
- 保存任务审计信息。

任务启动前必须满足：

- 急停已解除。
- App 持有控制权。
- CAN 与电机驱动就绪。
- 路径规划和控制节点就绪。
- 定位有效。
- 任务所需喷码机已就绪。
- 图纸与规划结果检查通过。

## 9. 喷码机控制

喷码机控制位于设置页面。

- 自动读取后端上报的已配置喷码机。
- 显示喷码机连接状态、启用状态和在线状态。
- 启用或禁用喷码机自动连接。
- 设置喷码机是否参与任务。
- 支持蜂鸣。
- 支持开始喷印和停止喷印。
- 支持清洗喷头。
- 支持测试喷印。
- 支持读取墨量等状态。
- 支持发送经过 JSON 格式校验的原始喷码命令。
- 未取得控制权或喷码机节点未就绪时阻止硬件操作。
- 喷码节点在线但所有喷头均未连接时，状态页显示喷码能力不可用，并阻止需要喷头的任务。

## 10. AI 助手

### 10.1 对话与配置

- 支持 DeepSeek 作为主要工具调用模型。
- 支持配置 DeepSeek、OpenAI、Claude、Gemini、Qwen、Kimi、GLM、MiniMax 和本地诊断模式。
- API Key 只保存在小车 FastAPI 后端，不写入 APK。
- 支持切换模型和重新配置 API Key。
- 显示每次会话的输入、输出和总 Token 数量。
- 本地最多保存 20 个会话。
- 每个会话最多保存 200 条消息。
- 请求模型时只携带有限的最近消息，避免上下文无限增长。
- 支持新建会话和切换历史会话。

### 10.2 状态诊断

- 查询后端和 ROS2 是否在线。
- 查询控制权、急停、CAN 和电机状态。
- 查询定位来源和定位有效性。
- 查询规划、任务和喷码机状态。
- 解释小车未就绪原因。
- 根据真实状态提供异常恢复建议。
- 不允许声称未上报的传感器已经正常。

### 10.3 设计能力

- 将自然语言需求参数化。
- 生成严格的 CAD JSON 图纸。
- 生成矩形等参数化图形。
- 对图纸执行可制造性检查。
- 将路径分为喷墨层和转场层。
- 输出 JSON 并解码为图形预览。
- 保存图纸版本。
- 进阶模式可以查看项目图纸版本历史，并从历史版本创建新的回退版本；不会覆盖原版本。
- 生成路径规划建议、任务摘要和验收信息。

### 10.4 AI 运动控制

- AI 可提出有限时直线、转向、组合运动和绕圈运动。
- 所有运动必须在 App 中二次确认。
- AI 运动开始后显示“停止 AI 控制小车运动”按钮。
- 显示 AI 运动剩余时间。
- 支持单段 `drive_robot` 运动。
- 支持多段 `drive_sequence` 顺序运动。
- 转向后前进等组合指令会作为一个运动序列提交，避免只执行第一段。
- DeepSeek 返回多个 `drive_robot` 时，后端会自动合并为 `drive_sequence`。
- 运动序列段数不设置业务上限。
- 运动序列总时长不设置业务上限。
- 每个单独动作仍必须指定有限时长，单段最长 30 秒。
- 长距离运动会自动拆分成多段连续执行。
- 每段执行期间持续检查急停、控制权、底盘状态和任务状态。
- 任一安全条件失效时立即停车并取消剩余步骤。

### 10.5 AI 工具安全

- JSON 工具参数使用严格 Schema 校验。
- 禁止额外未知字段。
- 运动、任务、LN150 和喷码机操作要求用户确认。
- 停车工具无需二次确认。
- 所有关键工具调用写入后端审计。
- 待确认操作有有效期，过期后必须重新发起。
- 工具执行结果保存为完成、失败、取消或过期状态。

### 10.6 进阶模式项目向导

- 进阶模式顶部显示项目当前阶段：需求、设计、规划、执行、验收。
- 没有项目时只提示用户描述设计需求，不显示执行类操作。
- 项目处于参数补充阶段时显示具体缺失参数。
- 项目处于候选设计、路径规划、执行确认、异常恢复或验收阶段时显示对应下一步。
- 项目步骤根据后端项目状态实时刷新，项目状态变化后进度条和操作建议同步更新。
- 该向导只负责引导和展示状态，不绕过用户确认、安全门禁或 ROS2 规划结果。

## 11. 持久化与审计

- 本地保存设备列表和当前设备。
- 本地保存 AI 会话记录。
- 后端保存 AI 配置。
- 后端保存待确认工具任务。
- 后端保存关键工具调用审计。
- 后端保存图纸版本信息。
- 后端保存任务账本、检查点、规划质量和验收报告。
- 进阶模式保存项目级 AI 成功/失败案例，基础模式运动演示不写入案例库。
- 可按项目查询 AI 案例，用于分析重复失败和后续设计参考。
- 后端登记实际设备能力、运动限制和 ROS2 主题/服务接口；登记信息用于进阶模式设计与规划参考。
- 设备能力登记包含模型、软件版本、底盘限制、定位能力、已配置接口和当前 ROS2 图中发现的接口。
- 数据库提供 schema 迁移状态、SQLite 一致性备份和审计筛选；这些历史运维数据不参与实时控制。
- 数据库能力登记和历史审计不能替代实时 ROS2/CAN 状态；急停、控制权、电机和任务门禁始终读取实时状态。
- 项目详情接口提供项目时间线，关联图纸版本、规划任务、执行任务和执行分段。
- 后端重启后默认保持软件急停状态，不会自动恢复运动。
- 进阶模式检测到任务账本中断时，会将项目标记为可恢复失败，保留最后已验证分段；必须重新规划并由用户确认后才能继续。

## 12. 后端运行与开机自启

- 后端部署目录：`/home/qingz/xline_app_backend`。
- ROS2 工作空间：`/home/qingz/xline_cyg`。
- 后端实际加载：`/home/qingz/xline_cyg/install_app/setup.bash`，与当前 `xline-cyg-runtime.service` 使用的环境一致。
- FastAPI 监听：`0.0.0.0:8000`。
- systemd 服务：`xline-app-backend.service`。
- 后端服务支持开机自启。
- `xline-cyg-runtime.service` 负责开机启动 ROS2 运行环境；`xline-app-backend.service` 依赖该服务并负责启动 FastAPI。
- 后端不会修改 `xline_cyg/src` 源代码。
- `odom_imu_localization.py` 的部署产物会在启动时检查可执行权限。

当前后端启动脚本、环境模板和默认配置已经统一使用 `install_app`。小车同时保留其他历史构建目录，但当前运行服务不使用它们。

## 13. 当前模式

AI 助手和图纸编辑器的 AI 图纸描述输入框均支持高精度离线中文语音转文字。点击麦克风开始录音，再次点击结束录音并由 App 内置 Zipformer-CTC int8 模型在本机转写；结果只写入当前输入框，允许修改后再发送或生成草图，不会自动执行机器人操作或覆盖图纸。首次使用会复制约 350 MB 的本地模型并请求录音权限，后续使用不依赖 HarmonyOS 系统语音服务或互联网。HarmonyOS 平板以 48 kHz 单声道 WAV 保存原始录音，模型根据 WAV 中的真实采样率自行重采样，避免实时 16 kHz PCM 流失真；识别前还会校正直流偏移和较低音量，转写后会修正常见的小车、喷码机、全站仪、规划路径和 ROS2 术语同音错误。

### 基础模式

- 面向简单运动控制，不创建项目或进阶规划任务；喷墨默认关闭。
- AI 将用户的自然语言运动要求转换为运动 JSON。
- 支持前进、后退、左转、右转、原地旋转、弧线和多段组合运动。
- 用户确认后调用后端运动工具，再通过 `/tablet_cmd_vel` 交给 `xline_cyg`。
- 支持连续提交多次运动，但上一条运动序列执行期间不会覆盖当前序列。
- Agent 顶部提供独立的“喷墨”开关；用户手动打开后，可边遥控或执行基础运动演示边喷墨。
- 用户明确说“打开喷墨/边走边喷/停止喷墨”时，AI 会生成 `printer_spray` 待确认操作；确认后才改变喷墨状态。
- 基础模式喷墨只使用持续喷墨控制，不创建项目、不生成图纸，也不执行精确喷墨路径。
- App 断开、控制权超时、急停或喷码机失联时，后端会自动停止喷墨。

### 进阶模式

- 面向设计到执行的完整流程。
- 包含需求参数化、候选方案、图纸版本、可制造性检查、路径规划、用户确认、分段执行、异常恢复和验收报告。
- 路径规划和路径跟踪交给 `xline_cyg`，后端不自行模拟精确路径。
- 规划任务必须满足实际 ROS2 节点和定位条件，不能只根据数据库缓存状态放行。

## 14. 主要后端接口

- `GET /health`：健康检查。
- `GET /api/status`：统一小车状态。
- `POST /api/cmd_vel`：速度控制。
- `POST /api/emergency-stop`：软件急停。
- `GET /api/drawings`：图纸列表。
- 图纸读取、保存、删除和版本管理接口。
- `POST /api/mission/control`：任务控制。
- 路径准备、执行、质量和报告接口。
- `POST /api/printer/quick_command`：喷码机快捷控制。
- 喷码机启用、参与任务和原始 JSON 命令接口。
- `POST /api/ln150/command`：LN150 控制。
- `GET/POST /api/agent/config`：AI 配置。
- `POST /api/agent/chat`：AI 对话。
- `POST /api/agent/confirm`：确认或拒绝工具调用。
- `GET /api/agent/tools`：严格工具 Schema 列表。
- `GET /api/agent/cases`：查询进阶模式 AI 成功/失败案例。
- `GET /api/agent/device-capabilities`：查询设备能力和 ROS2 接口登记。
- `GET /api/agent/device-capabilities/{device_id}`：查询指定设备登记。
- `GET /api/database/summary`：查询数据库表计数和持久化健康状态。
- `GET /api/database/migrations`：查询数据库 schema 版本和迁移记录。
- `POST /api/database/backup`：创建包含已提交 WAL 数据的一致性备份。
- `GET /api/agent/audit?event=&tool=&ok=`：按事件、工具和结果筛选审计记录。
- `WebSocket /`：App 实时状态和 ROS Bridge 兼容控制通道。
- `WebSocket /ws/status`：App 状态推送通道。
- `WebSocket /ws/rosbridge`：ROS Bridge 兼容通道。

## 15. 当前限制与注意事项

- 没有全站仪时使用相对定位，累计误差会随行驶距离增长。
- 全站仪可以提高绝对定位精度，但不能自动解决路径质量或控制参数导致的摆头问题。
- 障碍物检测能力取决于 `xline_cyg` 实际发布的数据和启用配置，App 不虚构传感器数据。
- App 软件急停不能替代物理硬件急停。
- 超长 AI 运动序列虽然不限制总时长和段数，但执行过程中必须保持 App 控制租约、后端连接和底盘状态正常。
- 实车测试应先架空车轮并确保硬件急停可立即触达，再从低速度和短时间开始。
- Flutter 测试、APK 构建和安装由项目使用者手动执行。
- 当前实车状态曾验证为：ROS2、CAN、底盘控制和相对定位有效；喷码节点存在，但喷头未连接时 `printer_ready=false`。
- `/imu` 使用 ROS2 sensor-data 的 `BEST_EFFORT` QoS，后端已匹配该 QoS，避免 IMU 数据因可靠性不兼容而丢失。
- 当前后端复查已确认 `/tablet_cmd_vel`、`/cmd_vel`、`/task_cmd_vel`、`/odom`、`/robot_pose`、`/plan_path`、`/execute_plan`、暂停/恢复服务、喷码服务和 LN150 服务与 `xline_cyg` 源码接口一致。
- `xline_cyg` 的部分规划配置仍可能引用历史 `/home/qingz/xline_ws3/...` 路径；后端保留兼容读取，直到小车 ROS2 代码统一修改路径。后端不会修改该 ROS2 工作区。
## 图纸编辑器坐标系

- 坐标网格覆盖整个编辑器视口，并根据当前缩放、平移和旋转矩阵动态计算可见范围。
- 缩小图纸、拖动画布或旋转视图时，坐标轴、刻度和网格会持续补齐，不受原有限画布边界限制。
- 图纸路径、小车原点标记与无限网格使用同一坐标变换，保持显示对齐。
- 编辑器顶部默认显示“双指平移、缩放和旋转画布”，并根据当前绘图工具补充单指操作提示。
