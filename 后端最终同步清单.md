# 小车后端最终同步清单

> 用途：只记录最终需要同步到小车 `/home/qingz/xline_app_backend/app/` 的后端文件。
> 每次修改后端代码后，必须更新本文件。前端文件、测试文件、临时文件和未纳入小车运行环境的文件不记录在这里。

## 同步目标

- 小车地址：`192.168.0.100`
- SSH 用户：`qingz`
- 后端目录：`/home/qingz/xline_app_backend/app/`
- 服务：`xline-app-backend`
- 服务端口：`8000`
- ROS2 运行代码：`xline_cyg`
- `xline_cyg`：禁止修改

## 当前最终版本

以下文件是当前后端运行版本中需要同步到小车的文件：

| 本地文件 | 小车目标文件 | 修改用途 | 同步状态 |
|---|---|---|---|
| `backend/app/*.py` | `/home/qingz/xline_app_backend/app/*.py` | FastAPI、Agent、数据库、任务、图纸、安全门禁和 ROS2 适配完整运行模块 | 已同步并验证 |
| `backend/run_robot_backend.sh` | `/home/qingz/xline_app_backend/run_robot_backend.sh` | 加载小车实际使用的 `xline_cyg/install_app` 环境并启动 Uvicorn | 已同步并验证 |

## 必须成组同步的依赖文件

当修改以下后端文件时，应同时检查并同步其直接依赖，避免小车端版本不一致：

| 依赖文件 | 说明 |
|---|---|
| `backend/app/tooling.py` | 工具 Schema、工具目录和安全门禁；修改 Agent 工具参数时必须同步 |
| `backend/app/state.py` | 运行状态模型；修改状态字段或 `/api/status` 时必须同步 |
| `backend/app/creative_projects.py` | 项目状态机和数据库项目持久化；修改项目状态转换时必须同步 |
| `backend/app/database.py` | 数据库主存储；修改项目、规划、执行持久化时必须同步 |
| `backend/app/mission_ledger.py` | 任务账本和分段执行记录；修改任务记录时必须同步 |
| `backend/app/mission_quality.py` | 规划质量检查；修改规划门禁时必须同步 |
| `backend/app/mission_reports.py` | 任务验收报告；修改验收逻辑时必须同步 |
| `backend/app/task_store.py` | Agent 待确认动作和任务状态；修改确认执行流程时必须同步 |
| `backend/app/audit_store.py` | 工具调用和审计记录；修改审计逻辑时必须同步 |
| `backend/app/agent_skills.py` | 设计、参数化、可制造性和恢复 Skill；修改相关 Skill 时必须同步 |
| `backend/app/drawing_store.py` | 图纸目录和图纸解析；修改图纸加载时必须同步 |
| `backend/app/drawing_versions.py` | 图纸版本持久化；修改图纸版本时必须同步 |
| `backend/app/mission_analysis.py` | 任务摘要和路径分析；修改任务摘要时必须同步 |
| `backend/app/oscillation.py` | 航向摆头检测；修改摆头检测时必须同步 |
| `backend/app/trajectory_comparison.py` | 规划轨迹和实际轨迹对比；修改验收对比时必须同步 |

## 同步规则

1. 后端文件修改完成后，先在本地执行 Python 编译和后端测试。
2. 只同步最终运行需要的 `backend/app/*.py` 文件，不同步 `tests`、`demo_cad_test` 和临时文件。
3. 同步到小车后执行 `python3 -m py_compile`。
4. 重启 `xline-app-backend`，检查 `enabled`、`active` 和 `8000` 端口。
5. 检查 `/api/status`、Agent 接口和受影响的业务接口。
6. 每次同步后在本文件更新文件清单、版本状态和验证结果。
7. 不修改小车 `xline_cyg` ROS2 代码。

## 修改记录

### 2026-08-19：建立最终同步清单

- 新建本文件。
- 纳入当前已涉及小车运行的 Agent、API、Schema、项目闭环和 ROS2 适配后端文件。
- 仅作为后续最终同步记录使用。
- 本次未修改 `xline_cyg`。

### 2026-08-19：基础模式与进阶模式

- 修改后端：`backend/app/agent_service.py`、`backend/app/schemas.py`。
- 基础模式：只处理底盘运动，允许模型将任意运动图形拆成 `drive_robot`/`drive_sequence`，返回运动 JSON 和确认动作；不创建项目、图纸、路径规划或喷墨任务。
- 进阶模式：保留当前设计、规划、执行、恢复和验收 Agent 流程。
- 兼容：后端仍接受旧模式值 `chat`/`work`，分别映射为 `base`/`advanced`。
- ROS2 接口：使用现有底盘速度工具，未修改 `xline_cyg`。
- 小车同步：待本轮本地测试通过后同步 `agent_service.py`、`schemas.py`。

### 2026-08-19：本轮同步结果

- 本地验证：后端 72 项测试通过，Python 编译通过。
- 计划同步文件：`agent_service.py`、`schemas.py`。
- 同步结果：失败；`192.168.0.100:22` 连接超时，文件未标记为已写入小车。
- 小车后端：本轮未重启，避免在未确认文件替换成功时操作服务。
- `xline_cyg`：未修改。

### 2026-08-22：AI 规划路径区分全站仪与相对定位

- 修改前端：`lib/main.dart`。
- AI 宽屏规划地图根据后端 `localization_source` 自动切换：`ln150_imu` 显示全站仪全局坐标与实时车位姿；`odom_imu_relative` 显示相对坐标系，并将小车起始位姿显示为 `(0,0,0)`。
- 修改后端：`backend/app/ros_adapter.py`。
- 规划预览和正式执行在无全站仪时调用 `/localization/calibrate_pose`，将当前车位和车头设为相对坐标原点；无全站仪且无相对定位节点时直接阻止规划并返回明确错误。
- 小车同步：已将 `app/ros_adapter.py` 同步到 `/home/qingz/xline_app_backend/app/ros_adapter.py`，并重启 `xline-app-backend`；远端 SHA256 与本地一致，`/api/status` 返回 200，当前 `localization_source=odom_imu_relative`、`localization_valid=true`、`control_ready=true`；`xline_cyg` 未修改。

### 2026-08-19：进阶模式项目步骤向导

- 修改前端：`lib/pages/agent_page.dart`。
- 进阶模式新增需求、设计、规划、执行、验收五阶段进度条。
- 根据项目状态显示缺失参数和下一步操作；没有项目时不会显示执行步骤。
- 本次未修改后端，不需要同步到小车。
- `xline_cyg`：未修改。

### 2026-08-19：进阶模式图纸版本回退

- 修改后端：`backend/app/drawing_versions.py`、`backend/app/main.py`、`backend/app/schemas.py`。
- 修改前端：`lib/pages/agent_page.dart`。
- 新增图纸版本历史查询和回退接口；回退只复制历史内容生成新版本，不覆盖原版本，并校验项目归属。
- 基础模式不显示版本历史入口，也不加载图纸回退工具。
- 本地测试和 Flutter 验证待执行；后端文件需要同步到小车，前端需要重新构建 APK。
- `xline_cyg`：未修改。

### 2026-08-19：进阶模式任务状态恢复

- 修改后端：`backend/app/creative_workflow.py`。
- 后端启动后，执行任务账本会把未完成执行标记为 `interrupted`；项目同步器据此将项目转换为 `execution_failed`。
- 保留任务 ID、图纸文件、已完成分段数、最后已验证分段和中断原因。
- 中断项目只允许进入恢复评估和重新规划，不会自动恢复速度指令或继续喷墨。
- 本地 Python 编译通过，后端 78 项测试全部通过。
- 小车同步：待同步 `creative_workflow.py` 以及本清单中其他“本轮待同步”文件。
- `xline_cyg`：未修改。

### 2026-08-19：进阶模式 AI 成功/失败案例记忆

- 修改后端：`backend/app/database.py`、`backend/app/agent_service.py`、`backend/app/main.py`。
- 新增 `ai_case_memory` 数据表，保存项目级设计、规划、执行、恢复和验收工具结果。
- 新增 `GET /api/agent/cases`，支持按项目查询案例。
- 基础模式的运动和停车工具不会写入案例库；案例写入失败也不会影响工具执行和安全流程。
- 本地 Python 编译通过，后端 79 项测试全部通过。
- 小车同步：待同步本清单中标记“本轮待同步”的后端文件。
- `xline_cyg`：未修改。

### 2026-08-19：设备能力和 ROS2 接口登记

- 修改后端：`backend/app/database.py`、`backend/app/ros_adapter.py`、`backend/app/main.py`。
- 新增 `device_capabilities` 数据表，保存设备 ID、型号、软件版本、能力、运动限制、定位能力和最后发现时间。
- 后端从实际 `RobotState.snapshot()` 和 ROS2 图发现主题/服务；同时登记后端配置的接口和当前发现的接口，便于诊断接口缺失。
- 新增 `GET /api/agent/device-capabilities` 和 `GET /api/agent/device-capabilities/{device_id}`。
- 能力登记是持久化和诊断数据；安全门禁、急停、控制权和运动执行仍直接读取实时 ROS2/CAN 状态。
- 基础模式不接入项目、图纸、规划或案例流程；进阶模式可通过设备状态和能力登记了解真实设备约束。
- 新增数据库测试；本轮未修改 `xline_cyg`。
- 小车同步：待本地后端检查通过后同步 `database.py`、`ros_adapter.py`、`main.py`。

### 2026-08-19：数据库迁移、备份和审计增强

- 修改后端：`backend/app/database.py`、`backend/app/main.py`。
- 新增 `schema_migrations` 表和当前 schema 版本记录；现有 SQLite 数据库启动时自动补齐，不删除旧业务数据。
- 新增 SQLite 在线一致性备份，使用 SQLite backup API，包含 WAL 中已提交的数据。
- 新增 `GET /api/database/migrations` 和 `POST /api/database/backup`。
- 审计接口改为查询 SQLite 审计主存储，支持按 `event`、`tool`、`ok` 筛选；JSONL 文件仍保留为兼容的追加记录。
- 审计参数继续脱敏，API key、token 和 password 不写入明文；审计、备份和迁移状态不参与急停、控制权或运动放行。
- 本地验证：Python 编译通过，后端 82 项测试全部通过。
- 小车同步：待同步 `database.py`、`main.py`；若第 5 项尚未同步，还需同步 `ros_adapter.py`。
- `xline_cyg`：未修改。

### 2026-08-19：全量复查后的运行期修正

- 修改后端：`backend/app/ros_adapter.py`、`backend/app/audit_store.py`、`backend/app/main.py`。
- ROS2 主题/服务发现增加 2 秒缓存，避免 App 高频刷新状态时重复扫描 ROS2 图；运动和安全状态仍实时读取。
- 数据库备份文件名增加微秒，连续备份不会互相覆盖。
- JSONL 或 SQLite 审计写入失败时仅记录降级，不阻断 AI 工具调用、运动控制或急停流程。
- 新增审计降级测试；本地 Python 编译通过，后端 83 项测试全部通过。
- 小车同步：待同步 `ros_adapter.py`、`audit_store.py`、`main.py`；前面第 5、6 项标记的其他后端文件也必须一并同步。
- `xline_cyg`：未修改。

### 2026-08-20：小车端首次手动同步结果

- 本地后端验证：Python 编译通过，83 项测试全部通过。
- 已成功写入小车：`agent_skills.py`、`audit_store.py`、`config.py`、`creative_projects.py`、`drawing_store.py`、`drawing_versions.py`、`mission_analysis.py`、`mission_ledger.py`、`mission_quality.py`、`mission_reports.py`、`oscillation.py`、`ros_adapter.py`、`state.py`、`task_store.py`、`tooling.py`、`trajectory_comparison.py`、`__init__.py`。
- 写入失败：`agent_service.py`、`creative_workflow.py`、`database.py`、`main.py`、`schemas.py`、`run_robot_backend.sh`，原因是小车端目标文件/脚本没有普通用户写权限。
- 小车现状检查：`xline-app-backend` 在线，8000 端口正常，`/api/status` 正常；但 `/api/database/migrations` 返回 404，证明远端仍是旧 `main.py`，当前文件版本混用，不能作为最终版本运行。
- 下一步：通过 `/tmp` 上传后使用 `sudo install -o qingz -g qingz` 覆盖上述失败文件，远端编译并重启服务后再次检查全部新接口。
- `xline_cyg`：未修改。

### 2026-08-20：实车后端恢复与 IMU QoS 修正

- 整套 `backend/app/*.py` 已覆盖到 `/home/qingz/xline_app_backend/app/`，修复新旧模块混用和 `DrawingVersionRollbackRequest` 导入失败。
- 后端启动环境与实际 `xline-cyg-runtime` 统一为 `/home/qingz/xline_cyg/install_app/setup.bash`；只修改后端配置和启动脚本，未修改 `xline_cyg`。
- 数据库迁移接口、设备能力接口和状态接口均返回 200；数据库 schema 版本为 3，ROS2、控制和相对定位状态正常。
- 发现 `/imu` 发布端为 `BEST_EFFORT`，后端旧订阅为 `RELIABLE`，导致 QoS 不兼容且无法接收 IMU；修改 `backend/app/ros_adapter.py` 使用 ROS2 sensor-data QoS。
- 待同步并验证：`ros_adapter.py`；验证后清理小车后端目录中不被 systemd 使用的旧 `backend.pid`、`backend.log` 和 `tests`。
- `xline_cyg`：未修改。

### 2026-08-19：修复基础模式组合运动缺段

- 修改文件：`backend/app/agent_service.py`、`backend/app/ros_adapter.py`。
- 问题：用户输入“前进1米后左转90度再前进1米”时，旧解析只保留转向和后一个前进动作，导致第一段运动丢失。
- 修复：基础模式按自然语言出现顺序提取全部前进、后退、左转和右转短语，完整生成 `drive_sequence`；用户确认后按顺序调用 ROS2 底盘工具。
- 多次任务：上一条运动完成或停止后可以继续提交下一条；运动进行中会拒绝新序列，不覆盖当前动作。
- 本地验证：后端 73 项测试通过，Python 编译通过。
- 小车同步：待 SSH 恢复后同步 `agent_service.py`、`ros_adapter.py` 并重启 `xline-app-backend`。
- `xline_cyg`：未修改。

### 2026-08-19：基础模式统一运动 JSON

- 修改文件：`backend/app/agent_service.py`。
- 基础模式链路：用户路线 -> Agent/API 理解 -> `movement_command` JSON -> 严格运动 Schema -> `drive_robot`/`drive_sequence` -> ROS2 底盘速度接口。
- 待确认动作现在包含完整 `motion_json`，App 会在确认按钮旁展示最终提交的运动 JSON。
- 基础模式使用开环运动 JSON，允许执行非精确运动；进阶模式仍使用图纸 JSON 和 `xline_cyg` 路径规划，不混用两条链路。
- 小车同步：待 SSH 恢复后同步 `agent_service.py`，并重启 `xline-app-backend`。
- `xline_cyg`：未修改。

### 2026-08-19：按 xline_cyg 复查并修正旧后端配置

- 修正 `backend/app/config.py`、`backend/run_robot_backend.sh`、`backend/xline-agent.env.example`：统一使用 `/home/qingz/xline_cyg/install_ws3/setup.bash`，删除后端默认的不存在 `install_app` 路径。
- 更新 `backend/README.md` 中过期的 `install_app` 示例为 `install_ws3`；该文件仅是部署文档，不纳入小车运行文件同步清单。
- 修正 `backend/app/ros_adapter.py`：任务就绪必须同时具备 `cmd_vel_mux`、`path_planner`、`motion_control_center`（兼容旧已安装的 `base_controller`）和定位节点，避免 API 显示可执行但 `/execute_plan` 实际不存在。
- 确认 `/tablet_cmd_vel`、`/cmd_vel`、`/task_cmd_vel`、`/odom`、`/robot_pose`、`/localization/valid`、`/plan_path`、`/execute_plan`、`/execution/pause`、`/execution/resume`、喷码服务和 LN150 服务与 `xline_cyg` 源码一致。
- 确认后端运动限制与 `xline_cyg` 驱动配置一致：最大线速度 `0.20 m/s`、最大角速度 `0.40 rad/s`、最大电机转速 `30 rpm`、轮径 `0.09115 m`、轮距 `0.255 m`。
- `xline_cyg` 的 `planner.yaml` 仍硬编码 `/home/qingz/xline_ws3/other/planned_results`、`/home/qingz/xline_ws3/cad`。后端保留旧结果目录的兼容读取；该路径不是后端多余文件，不能删除，否则当前规划器输出无法被读取。
- 本地验证：`python -m compileall -q backend/app` 通过；从 `backend` 目录运行后端测试 74 项通过。
- 小车同步：本轮尚未 SSH 同步；待同步文件为本表中标记“本轮待同步”的文件。
- `xline_cyg`：未修改。

### 2026-08-20：小车后端最终同步验证

- 已将本地完整 `backend/app/*.py` 同步到 `/home/qingz/xline_app_backend/app/`，远端运行模块与本地一致。
- 修正旧 root 文件造成的版本混用；`schemas.py`、`main.py`、`database.py`、`agent_service.py` 和 `creative_workflow.py` 均已更新并归属 `qingz:qingz`。
- 后端启动脚本和 `config.py` 已统一使用小车 `xline-cyg-runtime` 当前实际运行的 `/home/qingz/xline_cyg/install_app/setup.bash`。
- `/imu` 订阅已使用 `BEST_EFFORT` sensor-data QoS，与北微 IMU 发布端一致；`/api/status` 已收到姿态、角速度和线加速度。
- 本地验证：Python 编译通过，83 项测试全部通过。
- 远端验证：`xline-app-backend` 为 `enabled`、`active`，`xline-cyg-runtime` 为 `active`，8000 端口正常，且只有一个 Uvicorn 后端进程。
- API 验证：数据库 schema 版本 3；设备能力登记 1 台；`online=true`、`ros_available=true`、`control_ready=true`、`localization_valid=true`。
- 日志验证：当前启动周期无 ImportError、Traceback、setup 缺失或 QoS 不兼容警告。
- 已清理确认不再使用的 `/home/qingz/xline_app_backend/backend.pid`、`backend.log` 和远端 `tests`；保留 `backups`、CAD、地图和离线 wheelhouse。
- `xline_cyg`：未修改。

### 2026-08-20：设备可用状态与前端门禁修正

- 修改后端：`backend/app/ros_adapter.py`。
- 修正定位校准可用性：ROS2 图中存在 `/localization/calibrate_pose` 且类型为 `std_srvs/srv/Trigger` 时，即使 `service_is_ready()` 尚未刷新，前端也能正确启用对应操作；实际执行仍由 ROS2 服务返回结果决定。
- 修正喷码机能力状态：只有喷码节点存在且至少一个喷头上报 `connected=true` 或 `is_online=true` 时，`printer_ready` 才为 `true`。
- 基础运动、急停、控制权和任务执行门禁逻辑不变；`xline_cyg` 未修改。

### 2026-08-21：修复规划结果中 `ink: null` 导致任务失败

- 修改后端：`backend/app/ros_adapter.py`。
- 原因：`xline_cyg` 返回的转场段可能显式包含 `"ink": null`；后端读取规划结果统计喷头时使用了 `item.get("ink", {}).get(...)`，显式 `null` 不会触发默认值，导致 `NoneType has no attribute 'get'`，任务停在“读取规划结果失败”。
- 修复：对喷墨配置先做字典类型判断；非喷墨/转场段不参与所需喷头统计；去重指纹解析也兼容 `ink: null`。
- 测试：`python -m compileall -q app` 通过；后端单元测试 84 项全部通过。
- 小车同步：本轮待同步 `ros_adapter.py`；同步后需重启 `xline-app-backend` 并重新执行一次规划任务验证。
- `xline_cyg`：未修改。

### 2026-08-21：无全站仪时以当前车头建立路径起点

- 修改后端：`backend/app/ros_adapter.py`。
- 无全站仪且定位源为 `odom_imu_relative` 时，开始规划预览或直接规划执行前，自动调用 `/localization/calibrate_pose`。
- 该调用完成后，当前小车位置重置为相对原点 `0,0`，当前车头方向重置为相对 `0`，随后才向 `/plan_path` 提交图纸；执行已预览任务时不重复重置，避免改变已确认路径基准。
- 如果相对原点服务不可用或返回失败，后端阻止规划并返回明确错误，避免在错误坐标系下行驶。
- 有全站仪时保持 `ln150_imu` 全局定位流程，不触发相对原点重置。
- 本地验证：Python 编译通过，后端测试 84 项全部通过。
- 小车同步：本轮待同步 `ros_adapter.py`，同步后重启 `xline-app-backend`；`xline_cyg` 未修改。

### 2026-08-21：补齐喷墨测试流程闭环

- 修改后端：`backend/app/ros_adapter.py`、`backend/tests/test_mission_safety.py`。
- 正式路径任务保持由 `xline_cyg` 自身控制喷墨：转场段不喷墨，到达喷墨段后执行 `start_print`，喷墨段结束、暂停、取消或失败时执行 `stop_print`；后端没有把 `simulate` 插入正式任务。
- 修复快速测试喷墨未自动收尾的问题：后端收到 `/printer/quick_command` 的 `test_print` 成功响应后，延迟 1 秒自动发送同一打印机的 `stop_print`。
- 用户手动点击停止、发送其他打印机动作或后端关闭时，会取消待执行的自动停止；重复测试请求使用代际标识，旧请求不会误停止新测试或正式打印。
- 自动停止失败、服务响应异常和测试流程失败均写入后端日志；前端现有“停止”操作仍可立即中止测试流程。
- 本地验证：`python -m compileall -q app` 通过；后端单元测试 87 项全部通过。
- 小车同步：本轮待同步 `ros_adapter.py`；同步后重启 `xline-app-backend`，并分别验证一次 `test_print -> 自动 stop_print` 与正式路径的 `start_print -> 喷墨行驶 -> stop_print`。
- `xline_cyg`：未修改。

### 2026-08-21：喷墨闭环后端已同步并复查

- 已通过 SSH 将本轮 `backend/app/ros_adapter.py` 覆盖到 `/home/qingz/xline_app_backend/app/ros_adapter.py`，远端 SHA256 与本地一致。
- 已保留远端覆盖前备份：`/home/qingz/xline_app_backend/app/ros_adapter.py.bak.before-sync`。
- 已完成远端 Python 编译并重启 `xline-app-backend`。
- 远端运行检查：`xline-cyg-runtime` 和 `xline-app-backend` 均为 `enabled/active`；8000 端口正常；Uvicorn 仅 1 个进程；`/api/status`、`/api/database/migrations`、`/api/agent/device-capabilities` 均返回 200；当前启动日志无错误、异常、导入失败或 QoS 警告。
- ROS2 检查：`differential_wheels_driver`、`cmd_vel_mux`、`motion_control_center`、`path_planner`、`odom_imu_localization`、`inkjet_printer_node` 和 `xline_app_backend` 均在线；关键主题和服务存在；`/localization/valid=true`；`can0` 为 `UP/LOWER_UP`。
- 喷码机状态复查：`/printer_status` 当前上报 `connected=false`、`is_online=false`、`enabled=false`，因此 `/api/status` 的 `printer_ready=false` 属于真实设备未连接/未启用状态，不是后端异常。
- 实车闭环状态：运动与路径规划链路已就绪；后端代码已部署，正式路径和测试喷墨的实际闭环仍需在喷码机连接启用后，并在车轮架空、急停可触达的条件下验证；未修改 `xline_cyg`。

### 2026-08-22：修正手动与 AI 运动方向并降低控制延迟

- 修改后端：`backend/app/agent_service.py`、`backend/app/ros_adapter.py`、`backend/app/main.py`。
- 方向约定统一为 xline_cyg 使用的标准 ROS2 约定：`linear.x > 0` 为前进，`angular.z > 0` 为左转；删除 AI 执行链路中的重复取反。
- 手动 App 方向修正位于 Flutter 前端 `lib/main.dart`，不属于小车后端同步文件；该文件需要随 APK 更新。
- `/tablet_cmd_vel` WebSocket 高速控制成功时不再逐条回传完整状态快照，只在失败时回执；普通控制、急停、任务和状态消息保持原有回执，避免控制消息排队造成延迟。
- AI 定时运动和多段运动续发周期从 100ms 调整为 50ms；保留控制权、急停、CAN 就绪和失联急停门禁。
- 测试：后端 `compileall` 通过，88 项单元测试通过。
- 小车同步：已同步 `app/agent_service.py`、`app/agent_skills.py`、`app/ros_adapter.py`、`app/main.py` 到 `/home/qingz/xline_app_backend/app/`，并重启 `xline-app-backend`；其中 `agent_skills.py` 是 `agent_service.py` 的同版本依赖，不能遗漏。
- 远端验证：`xline-app-backend` 为 `active/enabled`，8000 端口监听，`/api/status` 返回 200，`online=true`、`control_ready=true`；启动日志无 ImportError、Traceback 或其他错误。
- `xline_cyg`：未修改。

### 2026-08-22：修复喷码状态门禁、失败反馈与图纸网格显示

- 修改后端：`backend/app/ros_adapter.py`、`backend/tests/test_mission_safety.py`。
- 喷码机可执行状态统一要求 `connected=true`、`is_online=true`、`enabled=true`；仅有 ROS2 节点或单一连接字段时不再误报可用，避免点击测试后断开或无效喷墨。
- 补充后端喷码生命周期测试的真实状态字段，验证 `test_print -> 自动 stop_print`、手动停止取消自动停止以及失败测试不创建自动停止任务。
- 修改前端：`lib/main.dart`、`lib/pages/drawing_editor_page.dart`、`lib/pages/control_page.dart`。服务调用失败现在会显示明确提示，喷码服务错误会显示“喷码机操作失败”；图纸编辑器坐标网格已补齐画布左侧和上侧区域，控制页使用真实的连接中/未连接/已连接状态门禁。
- 本地验证：后端 `python -m compileall -q app` 通过；后端单元测试 88 项全部通过。
- 小车同步：本轮后端待同步文件为 `backend/app/ros_adapter.py`、`backend/app/main.py`；同步后重启 `xline-app-backend`，并确认车端只有一个 `inkjet_printer_node`、`/printer_status` 只有一个发布者，再验证喷码机连接后执行测试。
- 车端环境处理：已清理重复的旧 `xline_ws3` 喷码节点；没有修改 `xline_cyg` 源码。

### 2026-08-23：修正 xline_cyg 喷码状态兼容与车端后端依赖

- 修改后端：`backend/app/agent_skills.py`、`backend/app/ros_adapter.py`、`backend/app/main.py`；修改前端：`lib/pages/control_page.dart`。
- `xline_cyg` 的 `is_online` 是后台 ping 标志，TCP 已连接时其 ping 线程会暂停，因此不能作为喷码执行硬门禁；喷码执行改为要求实时上报的 `connected=true` 与 `enabled=true`，`is_online` 仅用于诊断。
- 喷码控制页面不再把 `connected=true/is_online=false` 显示成“连接中”，连接开关改为只读真实连接状态，自动连接开关继续负责请求车端重连；测试、停止和 JSON 命令按真实连接与启用状态开放。
- 车端后端已重新同步完整 `backend/app/*.py`，修复了 `agent_service.py` 与 `agent_skills.py` 版本不一致导致的 `drawing_template_geometries` 导入错误；同时修正 Agent 默认喷码机识别，不再把 xline_cyg 的 ping 标志 `is_online=false` 误判为不可用；`xline-app-backend` 已重启并监听 8000。
- 远端检查：已同步并重启 `xline-app-backend`；`inkjet_printer_node` 仅 1 个，`/printer_status` 仅 1 个发布者，喷码服务全部存在；当前 `/api/status` 已返回 `printer_ready=true`，喷码节点的 `connected=true`、`enabled=true`、`is_online=false` 按上述 xline_cyg 状态语义正常处理。
- `xline_cyg` 源码和配置未修改。

### 2026-08-23：修复喷码测试无反馈并增加服务可用性检查

- 修改后端：`backend/app/ros_adapter.py`。
- `call_printer()` 在发送 QuickCommand 前检查 `/printer/quick_command` 服务是否真正可用；服务不存在或检查异常时立即返回失败并写入后端日志，避免 App 点击后静默等待。
- 修改前端：`lib/main.dart`。
- 喷码测试、开始、停止和 JSON 指令现在会立即显示“正在发送”，收到后端成功回执会显示成功提示，收到失败回执会显示具体原因；离线点击会明确提示无法发送。
- 未修改 `xline_cyg` 源码；本轮需要同步到小车的后端文件为 `app/ros_adapter.py`。前端修改需重新构建 APK。

### 2026-08-23：增加手动遥控随车喷墨闭环

- 修改后端：`backend/app/main.py`、`backend/app/ros_adapter.py`。
- 新增 WebSocket `printer_spray` 操作：打开时先激活喷码机，再发送 `start_print`；关闭时先发送 `stop_print`，再取消激活。
- 打开喷墨后，底盘 `/tablet_cmd_vel` 遥控指令不会关闭喷墨，可边控制小车边喷墨；断开连接和急停仍由后端安全逻辑负责停止设备。
- 修改前端：`lib/utils/ros_messages.dart`、`lib/pages/control_page.dart`、`lib/main.dart`。
- 手动控制页的“启用”开关改为真正的“喷墨”开关，不再误调用自动连接设置；收到后端响应会显示喷墨已开启或失败原因。
- `xline_cyg` 源码未修改；本轮需要同步到小车的后端文件为 `app/main.py`、`app/ros_adapter.py`。前端修改需重新构建 APK。
- 追加安全处理：App/WebSocket 断开、失联急停时，后端会先停止所有已激活喷头的 `stop_print`，再取消喷墨激活，避免失联后继续喷墨。

### 2026-08-23：补齐 xline_cyg 要求的 simulate 喷墨触发

- 检查 `xline_cyg` 后确认：`start_print` 只让喷码机进入打印状态，真正的软件触发命令是 `simulate`（协议 `0x17`）；其 `test_print` 流程没有继续发送 `simulate`，所以可能出现服务成功但没有出墨。
- 修改后端：`backend/app/ros_adapter.py`、`backend/app/main.py`。
- 测试按钮在 `test_print` 成功后追加 `simulate`；手动喷墨在 `start_print` 后追加 `simulate`，保持与 `xline_cyg` 的协议流程一致。
- `xline_cyg` 源码未修改；本轮需要同步到小车的后端文件为 `app/main.py`、`app/ros_adapter.py`。前端无需再次修改，已有 APK 可继续使用。

### 2026-08-23：修复主页喷墨开关接线与手动喷墨内容加载

- 修改后端：`backend/app/main.py`、`backend/app/ros_adapter.py`、`backend/tests/test_mission_safety.py`；修改前端：`lib/main.dart`、`lib/pages/control_page.dart`。
- 根因一：主页“喷墨”开关错误调用了 `/printer/set_enabled`，该服务只控制自动连接，不会开始喷墨；现已改为调用 WebSocket `printer_spray`。
- 根因二：`enabled=true` 仅表示 xline_cyg 允许发送喷码指令，不代表正在喷墨；前端不再把 `enabled` 当作喷墨状态，后端新增只读 `spraying` 运行状态并随 `/printer_status` 推送。
- 手动开启喷墨时，后端先通过 `test_print` 加载默认测试内容并进入打印状态，成功后发送 `simulate` 连续软件触发；该流程不会使用测试按钮的一秒自动停止计时器，因此可与遥控行驶同时保持喷墨。
- 手动关闭、控制连接断开、控制权超时或急停时，继续发送 `stop_print` 并清除 `spraying`，随后取消喷码机激活状态。
- 主页连接状态改为明确的“已连接/未连接”标识，不再使用容易误读的禁用开关；顶部小车连接与喷码机 TCP 连接仍分别显示。
- 喷码控制页“停止”按钮有效：调用 `/printer/quick_command` 的 `stop_print`，最终向喷码机发送 `{"EU2L":{"setupEvent":0}}`；它停止喷墨，不负责断开喷码机 TCP。
- 本轮最终需要同步到小车的后端文件：`app/main.py`、`app/ros_adapter.py`。`xline_cyg` 源码未修改；前端修改需要重新构建并安装 APK。

### 2026-08-23：基础模式增加可确认的持续喷墨控制

- 修改后端：`backend/app/agent_service.py`、`backend/app/tooling.py`。
- 基础模式默认不喷墨；只有用户明确提出打开、边走边喷或停止喷墨时，才开放 `printer_spray` 工具并生成确认操作。
- `printer_spray` 打开流程复用现有手动喷墨闭环：激活喷码机、加载测试内容、发送 `simulate`，确认后保持喷墨，直到用户关闭或安全逻辑停止；关闭时发送 `stop_print` 并取消激活。
- 基础模式仍不创建项目、不生成图纸、不执行进阶路径规划；普通运动请求不会误触发喷墨。
- `xline_cyg` 源码未修改；本轮最终需要同步到小车的后端文件：`app/agent_service.py`、`app/tooling.py`。
