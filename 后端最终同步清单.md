# xline_ws3 版后端最终同步清单

> 本文件只记录 `xline_ws3` 版最终需要部署到小车的文件。2026-08-25 已完成首次完整同步和车端接口核验。

## 版本边界

- 当前 App/后端目录：`D:\xikao\test1`
- ROS2 参考代码：`D:\xikao\xline_ws3`（只读，禁止修改）
- 小车 ROS2 工作区：`/home/qingz/xline_ws3`
- ROS2 环境：`/home/qingz/xline_ws3/install/setup.bash`
- 旧 `xline_cyg` 后端目录：`/home/qingz/xline_cyg_app_backend`（仅归档，服务永久禁用）
- 新 `xline_ws3` 后端目录：`/home/qingz/xline_app_backend1`（已创建并投入运行）
- 本清单不适用于旧版 `xline_cyg` 小车。

## 最终同步文件

| 本地文件 | 小车目标文件 | 用途 | 状态 |
|---|---|---|---|
| `backend/app/*.py`（整个目录） | `/home/qingz/xline_app_backend1/app/` | FastAPI、Agent、数据库、任务、ROS2 与喷码完整运行代码；新建目录时不能只同步改动过的模块 | 已同步 |
| `backend/requirements.txt` | `/home/qingz/xline_app_backend1/requirements.txt` | Python 运行依赖版本 | 已同步 |
| `backend/run_robot_backend.sh` | `/home/qingz/xline_app_backend1/run_robot_backend.sh` | 加载 `xline_ws3/install`、导出规划根目录并从独立后端目录启动；不写入 ROS 工作区 | 已同步 |
| `backend/run_ws3_runtime.sh` | `/home/qingz/xline_app_backend1/run_ws3_runtime.sh` | 常驻启动 ws3 底盘、定位、规划、控制器和喷码节点 | 已同步 |
| `backend/xline-agent.env.example` | `/home/qingz/xline_app_backend1/xline-agent.env.example` | 独立生成 `/home/qingz/.config/xline-agent-ws3.env` 的部署模板 | 已同步 |
| `backend/xline-ws3-runtime.service` | `/etc/systemd/system/xline-ws3-runtime.service` | ws3 ROS2 运行层独立开机自启服务 | 已安装 |
| `backend/xline-app-backend1.service` | `/etc/systemd/system/xline-app-backend1.service` | FastAPI 服务，拉起并晚于 ws3 runtime 启动 | 已更新 |
| `backend/README.md` | `/home/qingz/xline_app_backend1/README.md` | 部署与运行说明 | 已同步 |

## 同批前端文件

以下文件不部署到小车后端，但需要包含在 `xline_ws3` 版 APK 中：

| 文件 | 用途 |
|---|---|
| `lib/main.dart` | 更新当前 ROS2 运行版本说明 |
| `lib/pages/mission_page.dart` | 相对定位任务以运行时启动位置为原点，不再依赖不存在的校准服务 |

## 本地验证

- Python 编译：通过（`python -m compileall -q app`）。
- 后端单元测试：105 项通过（`python -m unittest discover -s tests -p "test_*.py"`）。
- 前端静态分析：通过（`dart analyze lib/main.dart lib/pages/mission_page.dart`，无诊断）。
- Flutter test 与 APK 构建：由用户执行。
- 小车端验证：服务、端口、数据库、Agent 配置和 ROS2 接口检查通过；实车运动与真实喷墨尚未执行。

## 小车同步后检查

1. 确认 `/home/qingz/xline_ws3/install/setup.bash` 存在。
2. 编译检查：`python3 -m compileall -q /home/qingz/xline_app_backend1/app`。
3. 永久禁用 cyg/旧服务并移除 `/usr/local/sbin/xline-backend-switch`。
4. 执行 `systemctl daemon-reload`，启用并启动 `xline-ws3-runtime` 和 `xline-app-backend1`。
5. 检查两个服务、`8000` 端口和 `/api/status`。
6. 有全站仪模式确认 `system_test.launch.py` 使用 `enable_hardware:=true`。
7. 无全站仪模式确认常驻 `shape_painting_system`、`planner_node` 和 `base_controller_node`。
8. 检查 `/odom`、`/imu`、`/robot_pose`、`/localization/valid`、`/printer_status`。
9. 喷码机测试必须确认 `/printer_status` 未超过 5 秒、`connected=true`、`enabled=true`；驱动提供 `device_state/print_count` 时核验计数，否则以现场实际出墨验收。
10. 确认 API、Agent 和任务只暴露 `printer_center`；`left`、`right`、`all` 必须被拒绝。

## 2026-08-24 本地修改批次

- 已按 `D:\xikao\xline_ws3\a__doc` 和源码接口完成本地适配。
- 未修改 `D:\xikao\xline_ws3`。
- 未连接或同步任何小车。
- 未影响旧版 `xline_cyg` 小车后端。

## 最终同步前部署门禁

1. 等待 `xline_ws3` 最终版，并重新比对 `a__doc`、launch、节点、服务、Action 和喷码协议。
2. 后端目录已固定为 `/home/qingz/xline_app_backend1`，服务名已固定为 `xline-app-backend1.service`。
3. 当前只允许 ws3 服务运行；不得恢复 cyg 服务或后端切换脚本。
4. App 固定连接 ws3 后端的 `8000` 端口。
5. 上述条件完成并重新测试后，才允许创建远程目录和同步。

## 2026-08-24 接口收紧批次

- 底盘方向保持 ROS REP-103：`linear.x > 0` 前进，`angular.z > 0` 左转，前后端不额外反向。
- 最终轮驱不再固定裁剪线速度/角速度，协议边界为每轮 `500 RPM`、速度断流超时 `0.50 s`；App 手动线速度默认 `0.10 m/s`、上限 `1.00 m/s`，角速度上限 `0.40 rad/s`。
- xline_ws3 仅创建 `printer_center`，API、Agent、任务工具和演示状态均不再暴露左右喷头。
- `enabled` 表示允许发送喷码指令，`auto_connect` 表示自动连接，`spraying` 由后端手动喷墨流程维护。
- 独立部署目录和 systemd 服务已在本地定稿，尚未同步小车。

## 2026-08-24 最终版接口复核批次

- 后端 ROS2 节点默认命名为 `/xline_app_backend1`，状态上报 `runtime_profile=xline_ws3`。
- 区分轮驱协议边界 `500 RPM`、App 手动默认值 `0.10 m/s`、App 手动上限 `1.00 m/s` 与规划任务上限 `0.10 m/s`；轮驱断流超时为 `0.50 s`。
- 喷码就绪要求 5 秒内收到状态；测试和手动持续喷墨只有在 `device_state=1` 且 `print_count` 增长后才成功，失败最多重试 3 次并关闭喷墨。
- Agent 环境模板默认使用 `DEEPSEEK_API_KEY`，不再出现默认 DeepSeek 却只读取 OpenAI 密钥的配置错位。
- `xline_ws3` 目前正式可执行链为 `/plan_path` + `/execute_plan`；未实现的直接 `shape_painter` Action 不作为可用能力暴露。
- 本批次仍仅修改本地 `D:\xikao\test1`，没有修改 `xline_ws3`，没有 SSH 或同步小车。

## 2026-08-24 双后端互斥切换批次

- cyg 与 ws3 后端目录同时保留，但禁止同时运行。
- `xline-app-backend1.service` 声明与 cyg 后端、cyg runtime 和旧兼容服务冲突。
- 统一使用 `sudo xline-backend-switch ws3|cyg|status` 切换或检查，切换不会删除任何一套代码或数据库。

## 2026-08-25 首次 ws3 车端同步

- 已将完整后端同步到 `/home/qingz/xline_app_backend1`，并安装 `xline-app-backend1.service` 与 `/usr/local/sbin/xline-backend-switch`。
- 已将默认启动切换为 ws3，cyg 后端和 runtime 保留但禁用。
- 首次启动发现环境模板中的 `XLINE_VEHICLE_MODEL=XLine Rover` 未引用空格，导致 Bash 把 `Rover` 当作命令；本地模板和远端 ws3 环境已改为 `XLINE_VEHICLE_MODEL="XLine Rover"`。
- `xline-app-backend1.service` 已启用且运行，`0.0.0.0:8000` 正常监听；cyg 两项服务保留但已禁用、停止。
- `/health`、数据库迁移、Agent 配置和设备能力接口正常；DeepSeek 已配置，数据库 schema 为 3。
- ROS2 后端节点为 `/xline_app_backend1`，设备 ID 为 `xline-ws3-01`；运动限制为默认 `0.10 m/s`、手动上限 `1.00 m/s`、任务上限 `0.10 m/s`、角速度上限 `0.40 rad/s`。
- 当前安全急停保持，未进行底盘运动或真实喷墨验收；喷码功能是否实际出墨仍需按安全流程检查 `device_state` 与 `print_count`。
- 未修改 `/home/qingz/xline_ws3`、`/home/qingz/xline_cyg` 或 cyg 后端代码。

## 2026-08-25 ws3 运行环境启动恢复

- 现场表现为 App 已连接，但底盘、定位、规划和喷码均未就绪，解除急停返回“底盘运行环境未就绪”。
- 车端运行日志确认旧启动曾错误加载不存在的 `/home/qingz/xline_ws3/install_ws3/setup.bash`；当前独立环境已使用正确的 `/home/qingz/xline_ws3/install/setup.bash`。
- 修改 `backend/app/ros_adapter.py`：检查 runtime PID 时同时核验 `/proc/<pid>/cmdline` 确为 ws3 launch，失效或被其他进程复用的 PID 自动清理，避免解除急停一直等待错误进程。
- `backend/app/ros_adapter.py` 已同步，车端编译通过并重启 `xline-app-backend1.service`；失效 PID 和旧 runtime 日志已清理，确认环境加载 `/home/qingz/xline_ws3/install/setup.bash`。不修改 `xline_ws3` 源码。
- 二次现场复查发现解除急停在进入 `set_emergency_stop(false)` 前，被 `control_ready=false` 提前拒绝，导致 runtime 永远没有机会启动。
- 修改 `backend/app/tooling.py`：解除急停的前置门禁改为控制权、ROS2 后端和 CAN 接口；进入状态转换后仍由 `ros_adapter` 启动 runtime，并必须等轮驱与速度仲裁节点就绪才能真正解除急停。
- `backend/app/tooling.py` 已同步，车端编译与服务重启通过；该修改不降低运动工具和任务执行对 `control_ready` 的门禁要求。服务重启后需由 App 重新取得控制权并再次点击解除急停，才能现场验证 runtime 启动结果。
- 现场再次验证后，App、`/tablet_cmd_vel`、`cmd_vel_mux` 和轮驱订阅链路均正常，后端收到摇杆速度；不能移动的剩余根因位于 `can0` 物理总线。
- `can0` 为 1 Mbit/s、非 LOOPBACK，且已临时把发送队列调为 1000，但 TX 成功计数仍为 0，`bus-off` 持续增长，轮驱反复报告 `[Errno 105] No buffer space available` 和 `0x097/0x098` 反馈超时；软件急停已重新锁定。
- 在确认电机驱动器上电、CAN-H/CAN-L、共地、两端 120 欧终端电阻和收发器发送侧恢复前，禁止继续运动测试。该故障不能通过放宽后端安全门禁解决。

## 2026-08-25 喷墨启动状态反馈

- 最终需同步 `backend/app/main.py` 和 `backend/app/ros_adapter.py`。
- 手动喷墨新增 `starting`、`spraying`、`stopping`、`idle`、`error`、`disconnected` 状态，并在 `/api/status` 与 WebSocket 状态中持续返回。
- 点击开启后立即进入 `starting`，但仍以 `device_state=1` 且 `print_count` 增长作为真实喷墨成功条件；最多三次触发仍未确认时停止打印并返回明确错误。
- 停止喷墨进入 `stopping`，ROS2 服务确认后进入 `idle`；服务失败进入 `error`，不会把“指令已发送”误报为“已经出墨”。
- 前端配套修改位于 `lib/main.dart` 和 `lib/pages/control_page.dart`，需要重新构建并安装 APK 才能看到转圈及“正在开启/正在关闭”反馈。
- 本地 Python 编译通过，后端 106 项单元测试通过；前端 `dart analyze lib/main.dart lib/pages/control_page.dart` 无诊断。

### 车端同步结果

- 已同步 `backend/app/main.py` 和 `backend/app/ros_adapter.py` 到 `/home/qingz/xline_app_backend1/app/`。
- 覆盖前备份位于 `/home/qingz/xline_app_backend1/backups/20260825-014356/`。
- 车端 Python 编译通过，`xline-app-backend1.service` 为 `enabled` 且 `active`，`0.0.0.0:8000` 正常监听，`/health` 返回 200。
- 本地与车端两个文件的 SHA-256 完全一致；cyg 后端保持 `inactive`，未修改 `xline_ws3` 或 `xline_cyg`。
- 服务重启按安全策略保持急停，并结束旧后端拉起的 ROS2 子进程；需由 App 重新取得控制权并解除急停后再启动 ws3 runtime，未远程代替用户解除急停。

## 2026-08-25 喷码激活竞态修复

- 现场确认 App、后端和喷码机 TCP 均已连接，失败并非连接故障；`/api/status` 返回 `connected=true`、`enabled=false`，真实错误为“printer_center 已禁用，不允许发送指令”。
- xline_ws3 的 `/printer/set_active` 服务约需 1 秒完成，旧后端固定等待 `0.35 s` 就发送 `test_print`，导致激活尚未生效时指令被拒绝。
- 修改 `backend/app/main.py` 和 `backend/app/ros_adapter.py`：发送打印指令前最多等待 3 秒，只有 `/printer_status.enabled` 达到目标值才继续；超时进入 `error` 并显示“喷码机激活超时”。
- 本地 Python 编译通过，后端 107 项单元测试通过。
- 已同步到 `/home/qingz/xline_app_backend1/app/`，车端备份位于 `/home/qingz/xline_app_backend1/backups/20260825-015406/`。
- 车端编译通过，`xline-app-backend1.service` 为 `active`，`/health` 返回 200；未修改 `xline_ws3`。

## 2026-08-25 喷墨与底盘并行及热激活修复

- 最终需同步 `backend/app/main.py`、`backend/app/ros_adapter.py` 和 `backend/app/agent_service.py`。
- 喷墨开启的激活、状态等待、内容加载和真实触发全部在后台线程执行，不再占用 WebSocket 消息处理线程；开启过程中底盘速度消息可继续按原频率发送。
- 手动关闭、Agent 关闭、控制权释放、WebSocket 断开和急停清理只发送 `stop_print` 并清除喷墨状态，不再反复调用 `set_active(false)`；喷码机保持已激活待命，下次开启可跳过约 1 秒的激活服务。
- 安全策略不变：失联和急停仍立即停止底盘并发送停墨指令；保留喷码指令权限不等于允许自动喷墨，喷墨操作仍受连接、控制权和状态门禁约束。
- 后台开启在发出打印指令前再次确认状态仍为 `starting`；若期间已关闭、急停或断线，则取消迟到的开启，避免停墨后重新触发。
- Agent 基础模式使用相同的热激活和停墨逻辑，不再因一次关闭把喷码机重新禁用。
- 前端配套修改为 `lib/components/common_components.dart`：操纵杆按首根触控 pointer 独立跟踪，第二根手指点击喷墨开关不会取消遥控或发送意外零速度；该前端修改需重新构建 APK。
- 本地 Python 编译通过，后端 107 项单元测试通过；`dart analyze lib/components/common_components.dart` 无诊断。

### 车端同步结果

- 已同步 `backend/app/main.py`、`backend/app/ros_adapter.py` 和 `backend/app/agent_service.py` 到 `/home/qingz/xline_app_backend1/app/`。
- 覆盖前备份位于 `/home/qingz/xline_app_backend1/backups/20260825-023507/`。
- 车端 `python3 -m compileall -q app` 通过；三份文件本地与车端 SHA-256 完全一致。
- `xline-app-backend1.service` 已恢复为 `enabled` 且保持 `active`，`0.0.0.0:8000` 正常监听，`/health` 返回 200。
- 本次启动日志未发现 Traceback、ImportError、Exception 或启动失败；未修改 `xline_ws3`，未解除急停，未执行底盘运动或真实喷墨。
- 前端操纵杆多指并行修复仍需重新构建并安装 APK 后才能生效；仅同步后端不会改变当前已安装 APK 的触控行为。

## 2026-08-25 修复 ws3 缺少喷墨计数导致的误失败

- 现场状态确认喷码机实际为 `connected=true`、`enabled=true`，失败原因是当前 ws3 驱动未填充 `device_state` 和 `print_count`；旧逻辑因此连续触发三次后误判“未检测到真实喷墨”，并主动发送 `stop_print`。
- 修改 `backend/app/ros_adapter.py`：驱动提供设备状态与打印计数时继续严格核验；字段不可用时只发送一次 `simulate`，以已成功受理的 `test_print` 和触发提交作为当前接口能够提供的确认，不再因缺少遥测重复触发并自动停墨。
- 真实连接断开、激活失败、打印内容加载失败、喷墨服务无法提交、急停和控制失联仍进入错误或安全停墨，不放宽这些安全门禁。
- 新增缺少 `device_state/print_count` 时保持手动喷墨状态的回归测试；本地 Python 编译、完整单元测试和新增定向测试均通过。

### 车端同步结果

- 已同步 `backend/app/ros_adapter.py` 到 `/home/qingz/xline_app_backend1/app/`，覆盖前备份位于 `/home/qingz/xline_app_backend1/backups/20260825_031411/`。
- 远端编译通过，本地与车端 SHA-256 均为 `8bb7ed6eaf9dcc1ef6b61ef4a4ca7a8d7ee157a9c6d49cb75b87cecf67b3a107`。
- `xline-app-backend1.service` 重启后为 `active`，`/health` 返回 `runtime_profile=xline_ws3`；未修改 `xline_ws3` 源码。
- 服务重启按安全策略结束了原后端进程组中的 ws3 硬件节点，目前保持软件急停，需用户在车旁通过 App 点击“解除急停”重新启动硬件运行层；没有远程代替用户解除急停或触发真实喷墨。

## 2026-08-25 固定 ws3 单版本与运行层开机常驻

- 删除本地 `backend/switch_xline_backend.sh`，车端删除 `/usr/local/sbin/xline-backend-switch`；不再提供 ws3/cyg 后端切换功能。
- `xline-cyg-runtime.service` 与 `xline-cyg-app-backend.service` 已设为 `disabled/inactive`，旧目录仅作为归档保留。
- 新增 `backend/run_ws3_runtime.sh` 与 `backend/xline-ws3-runtime.service`，独立常驻启动底盘、IMU、速度仲裁、相对定位、喷码、规划器和运动控制器；服务设为 `enabled`、`Restart=always`。
- `xline-app-backend1.service` 使用 `Wants/After=xline-ws3-runtime.service`，开机自动拉起并晚于运行层进程启动；运行层异常时 FastAPI 保持在线报告未就绪，runtime 由 systemd 自动恢复。
- 修复 ROS Humble 环境脚本与 `set -u` 不兼容导致的启动循环；runtime 脚本使用 `set -eo pipefail`。
- 后端收到解除急停时，如果 systemd runtime 已运行，只等待节点就绪，不再自行拉起第二套 launch，避免重复底盘和喷码节点。
- 按最终 ws3 节点名补充 `drawing_planner_node` 规划器别名；车端最终状态为 `control_ready=true`、`mission_nodes_ready=true`、`missing_required_nodes=[]`、`localization_valid=true`、`printer_ready=true`。
- 隔离验证：单独重启 `xline-app-backend1.service` 前后，runtime 主 PID 均为 `22465`，ROS2 节点集合哈希一致，所有关键节点各一份；后端重启不再终止运行层。
- 两个 ws3 服务均为 `enabled/active`，软件急停仍保持 `true`，未远程解除急停、移动小车或触发真实喷墨。
- 部署前备份位于 `/home/qingz/xline_app_backend1/backups/20260825_032820/`；未修改 `/home/qingz/xline_ws3` 源码。

## 2026-08-25 修复 ws3 显示喷墨但实际未触发

- 现场日志确认打印内容、`start_print` 和一次 `simulate` 均已提交，但当前喷码器没有返回 `print_count`，旧后端因此仅凭一次命令提交就显示“喷墨中”。
- 修改 `backend/app/ros_adapter.py`：有 `device_state/print_count` 时继续严格按计数增长确认；计数不可用时依据 ws3 实机记录，以 `0.3 s` 间隔发送两次 `simulate`，提高实际连续喷印启动成功率。
- 第二次触发前检查操作代次；用户停止、断连或取消后不再继续发送延迟触发，避免已关闭后再次出墨。
- 计数不可用时状态改为 `triggered_unverified`，不再声称已确认真实出墨；前端 `lib/pages/control_page.dart` 对应显示“已触发·待确认”。该文案变更需重新构建 APK，双触发后端修复无需等待 APK 更新即可生效。
- 本地 `python -m compileall -q app` 通过；喷码与 ws3 定向测试 30 项通过；完整后端测试 111 项通过。
- 本次车端只需同步 `/home/qingz/xline_app_backend1/app/ros_adapter.py` 并重启 `xline-app-backend1.service`；不得重启 `xline-ws3-runtime.service`，不得修改 `/home/qingz/xline_ws3`。
- 已同步到车端并通过远端编译；备份为 `/home/qingz/xline_app_backend1/backups/20260825_034716/ros_adapter.py`。
- 本地与车端 `ros_adapter.py` SHA-256 均为 `67f70c4de8a221f4867cd123568b43682bcadc7b1dbe4f36df6b0790fcab9cdd`。
- `xline-app-backend1.service` 与 `xline-ws3-runtime.service` 均为 `active`，`/health` 返回成功；后端重启前后 runtime PID 均为 `22465`，未重启 ROS2 运行层，未远程触发喷墨。

## 2026-08-25 统一 AI/控制页喷墨状态并保留连续 AI 运动喷墨

- 检查确认 ws3 旧后端与 cyg 版存在同类逻辑：正常 AI 运动完成调用全局 `fail_safe_stop()`，导致底盘停车的同时发送 `stop_print` 并清除用户主动开启的喷墨状态。
- 修改 `backend/app/ros_adapter.py`：新增正常运动完成路径，只发布零速度、清理 AI 运动计时与序列，不改变喷墨状态；单段和多段 AI 运动正常完成均使用该路径。
- 急停、控制租约超时、App 断连、控制权丢失、运动指令拒绝、安全条件丢失及用户明确停止仍调用 fail-safe，同时停车并停止喷墨，安全门禁没有放宽。
- 修改 `lib/pages/agent_page.dart`：AI 基础模式喷墨开关与控制页使用同一后端操作和状态语义，显示“正在开启”“喷墨中”“已触发·待确认”“正在关闭”“操作失败”，操作期间禁用重复点击。
- 本地 Python 编译通过；喷码、AI 运动生命周期与 ws3 兼容定向测试 32 项通过；完整后端测试 113 项通过。
- 车端只需同步 `/home/qingz/xline_app_backend1/app/ros_adapter.py` 并重启 `xline-app-backend1.service`；AI 页界面变化必须重新构建并安装 APK 才能显示。
- 已同步并通过远端编译；备份为 `/home/qingz/xline_app_backend1/backups/20260825_035719/ros_adapter.py`。
- 本地与车端 SHA-256 均为 `956d22f5de8cb1602d768d2c7857b640dee4d8aa77e5369a8417b1f0298cd153`；两个 ws3 服务均为 `active`，`/health` 正常。
- 后端重启前后 runtime PID 均为 `22465`，未重启 ROS2 运行层，未远程解除急停、移动小车或触发真实喷墨。

## 2026-08-25 修复 ws3 喷墨假开启与开启永久等待

- 最终需同步 `backend/app/main.py` 和 `backend/app/ros_adapter.py` 到 `/home/qingz/xline_app_backend1/app/`；前端配套修改为 `lib/main.dart`，需重新构建 APK 才能获得 30 秒超时提示。
- 现场状态确认 ws3 已提供 `device_state`、`print_count` 和 `sub_print_count`，但旧后端仍可能出现 `enabled=true`、`spraying=false`、`spray_state=idle`；`enabled` 只表示允许发送喷码指令，不能再作为真实出墨依据。
- 手动喷墨改用 `xline_ws3/src/trajectory_painter/trajectory_painter/shape_painter.py` 的正式线条流程：停止旧任务、发送 `0x34` 内容装载模式、发送 `0x54` 的 `300x6` 黑线、发送 `0x34 interval=2.0`、`start_print`，再最多三次发送 `simulate`。
- 每个 ROS2 服务调用都等待最终 `success/message` 响应并设置超时；不再把 `call_async()` 已提交误报为成功。服务不回包、配置失败、触发失败或计数未增长时会发送 `stop_print` 并进入 `error`，不会永久停留在“正在开启”。
- ws3 提供遥测时，只有 `PrintCount` 增长且 `device_state=1` 才进入 `spraying`；缺少计数的旧固件仍保持 `triggered_unverified`，不会声称已确认出墨。
- 线条内容首次成功装载后在当前后端进程内缓存，后续关闭再开启跳过三次内容配置，只执行启动与触发确认；喷码机断线时自动清除缓存。
- 前端为每次开启/停止增加 30 秒兜底；超时会解除按钮锁定、显示具体错误，并在开启超时时补发停止保护。
- 新增正式 ws3 配置顺序、真实计数确认和服务超时回归测试；本地 Python 编译通过，定向测试 34 项、完整后端测试 115 项通过，`dart analyze lib/main.dart` 无诊断。
- 本节同步后必须只重启 `xline-app-backend1.service`，不得重启 `xline-ws3-runtime.service`，不得修改 `/home/qingz/xline_ws3`，不得远程触发真实喷墨。

### 车端同步结果

- 已同步 `main.py` 和 `ros_adapter.py`；覆盖前备份位于 `/home/qingz/xline_app_backend1/backups/20260825_042142/`。
- 车端 Python 编译通过，`xline-app-backend1.service` 与 `xline-ws3-runtime.service` 均为 `active`，`/health` 返回 `runtime_profile=xline_ws3`、`control_ready=true`、`printer_ready=true`。
- ws3 runtime 主 PID 重启后端前后均为 `2198`，未重启运行层；ROS2 节点无重名，`/printer_status` 只有一个发布者 `inkjet_printer_node`。
- `main.py` 本地/车端 SHA-256 均为 `e6b56ba0014c962656ee3df71c9ae5ed64e7040c20fb4b0da3b36f0ea2effb50`；最终 `ros_adapter.py` 均为 `76a0f350d5bb4f30d07d5078dbdbe56c2cf22ab481177b18fe42440496f55765`，并发保护覆盖前备份为 `/home/qingz/xline_app_backend1/backups/20260825_042421/`。
- 后端重启后的首次状态上报已恢复为 `printer_ready=true`、`connected=true`、`enabled=true`、`spray_state=idle`、`spray_error=""`；允许指令与实际未喷墨状态一致。
- 重启后保持软件急停，未远程解除急停、移动小车或触发真实喷墨；必须由现场人员放置测试材料并观察喷头后再验证开启。

## 2026-08-26 统一图形 Skill 优先使用 ws3 trajectory_painter

- 修改 `backend/app/agent_service.py` 的 Agent 工作规则：基础模式和进阶模式遇到直线、圆形、正方形或椭圆形等标准图形时，优先进入 `xline_ws3` 的 `trajectory_painter` 图形任务链；仅对自定义折线或现成包不支持的图形回退到本地 `drive_sequence`。
- 本次只修改 Agent 路由提示和策略，不修改 `xline_ws3` 源码，不新增车端文件；现有 `xline-app-backend1.service` 使用的后端文件需要同步 `backend/app/agent_service.py`。
- 同步后只需重启 `xline-app-backend1.service`；不得重启 `xline-ws3-runtime.service`，不得远程触发真实运动或喷墨。
## 2026-08-26 CAD 文件导入

- `backend/app/cad_import.py`：新增 DXF 线、折线、圆、圆弧、椭圆解析，统一转换为规划器使用的毫米制 CAD JSON。
- `backend/app/main.py`：新增 `POST /api/drawings/import-cad`，导入后按现有图纸版本库保存；DWG 暂要求先转换为 DXF，避免误解析。
- `backend/requirements.txt`：加入 `ezdxf==1.4.2` 与 `python-multipart==0.0.20`。同步车端时必须同步这两个依赖。
## 2026-08-26 监控 API 优先诊断

- `backend/app/schemas.py`：新增监控诊断请求模型。
- `backend/app/main.py`：新增 `POST /api/monitor/diagnose`。该接口必须先调用 Agent API 分析实时快照，API 返回后才允许本地规则补充诊断，不允许前端直接以本地规则替代 AI 判断。

## 2026-08-27 监控与任务阶段闭环接入

- `backend/app/telemetry_contract.py`：提供版本化 `telemetry`、喷码和任务合同，统一 App 读取方式。
- `backend/app/main.py`：`/api/status` 输出合同，并新增 `GET /api/telemetry`。
- `backend/app/state.py`：保留真实能源字段；未由 ws3 上报的电压、电流、温度继续为空。
- `lib/main.dart`：连接后每 2 秒读取 `/api/telemetry`，断开时清除缓存，REST 失败回退 WebSocket 状态。
- `lib/pages/monitor_page.dart`：能源监控优先显示合同中的真实数据。
- `lib/pages/mission_page.dart`：增加准备、规划、确认、分段执行、验收时间线及失败/定位锁定提示。
- 尚未同步车端；同步至少包括 `main.py`、`state.py`、`telemetry_contract.py`，前端变化需重新构建 APK。

## 2026-08-27 统一遥测合同、喷码生命周期与任务阶段

- `backend/app/telemetry_contract.py`：新增版本化遥测合同，统一运行层、底盘、定位、能源、障碍物和喷码字段；未由真实设备上报的电压、电流、温度和墨量保持 `null`。
- `backend/app/state.py`：补充电压、电流、温度及能源来源字段，继续保留现有顶层字段以兼容旧 App。
- `backend/app/main.py`：`/api/status` 增加 `telemetry`、`printer_contract`、`mission` 三个结构化对象；新增 `GET /api/telemetry`，用于监控页读取统一合同。
- `lib/pages/mission_page.dart`：任务执行区改为“准备、路径规划、执行确认、分段执行、验收”阶段时间线，并显示分段进度与定位锁定提示。
- 喷码生命周期继续使用真实状态 `starting/stopping/spraying/triggered_unverified/error`，不得仅依据开关值声称喷墨成功。
- 本次修改尚未同步车端；后续同步至少包括 `backend/app/main.py`、`backend/app/state.py`、`backend/app/telemetry_contract.py`，并按依赖检查重启对应后端服务，不重启 ws3 运行层。
