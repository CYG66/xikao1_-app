# XLine 划线小车连接全步骤

本文档适用于以下环境：

```text
小车系统：Ubuntu 22.04 + ROS2 Humble
小车用户：qingz
小车 IP：192.168.0.100
ROS2 工作空间：~/xline_ws
App 后端：~/xline_app_backend
后端端口：8000
```

> 安全原则：App 显示“已连接”只代表 WebSocket 网络连接成功，不代表电机、定位和喷码设备已经就绪。只有 `/health`、ROS2 节点和电机串口全部正常后，才允许发送运动指令。

## 1. 连接同一网络

1. 启动小车 Ubuntu 系统。
2. 将电脑、测试手机或 Android 模拟器与小车连接到同一个局域网。
3. 在 Windows PowerShell 中执行：

```powershell
ping 192.168.0.100
```

应收到小车回复且无丢包。如果超时，先检查小车 IP、Wi-Fi、路由器和电脑防火墙。

检查 SSH 端口：

```powershell
Test-NetConnection 192.168.0.100 -Port 22
```

`TcpTestSucceeded` 应为 `True`。

## 2. SSH 登录小车

```powershell
ssh qingz@192.168.0.100
```

根据提示输入 SSH 密码。不要将密码写入项目或本文档。

建议为以下操作分别打开两个 SSH 终端：

- 终端 A：检查或启动 ROS2 节点。
- 终端 B：启动 FastAPI Bridge。

不要在持续输出 ROS2 日志的终端中输入 `ros2 node list` 等命令，应另开一个 SSH 终端。

## 3. 加载 ROS2 环境

每次打开新的小车终端都要执行：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws/install/setup.bash
```

验证环境：

```bash
echo $ROS_DISTRO
ros2 pkg list | grep xline
```

正确结果应包含：

```text
humble
xline_base_controller
xline_bringup
xline_cmd_vel_mux
xline_ln150_driver
xline_localization
xline_path_planner
```

## 4. 启动前检查 USB 与串口

执行：

```bash
lsusb
ls -l /dev/ttyUSB* /dev/ttyACM* 2>/dev/null
ls -l /dev/serial/custom 2>/dev/null
```

当前已经确认的实际映射为：

| 硬件 | 实际串口 | 固定别名 | 说明 |
| --- | --- | --- | --- |
| 北微 IMU | `/dev/ttyUSB0` | `/dev/serial/custom/imu` | FTDI FT232R，序列号 `B000Z32T` |
| HDSC USB2CAN | `/dev/ttyACM0` | `/dev/serial/custom/wheels_usb2can` | HDSC CDC，序列号 `00000000050C` |

当前电机配置文件位于：

```text
~/xline_ws/src/xline_driver/wheels_driver/config/differential_wheels_params.yaml
```

其中原配置为：

```yaml
left_serial_port: "/dev/ttyUSB0"
right_serial_port: "/dev/ttyUSB2"
```

该配置目前不能安全使用，原因是：

- `/dev/ttyUSB0` 已确定属于 IMU，不能再分配给左轮。
- `/dev/ttyUSB2` 当前不存在。
- 当前 `run_motor` 使用 YZ-AIM M5705G10 的双路 Modbus RTU 驱动。
- 已连接的 `/dev/ttyACM0` 是 HDSC USB2CAN，与 Modbus RTU 不是同一种协议。

在确认电机控制器型号和接线方案前，不要启动 `wheels_driver`，也不要在 App 中发送控制指令。

### 方案 A：YZ-AIM 双路 Modbus RTU

如果电机确认为 YZ-AIM M5705G10，应连接两只独立 USB-RS485 转换器。连接后系统应新增两个串口，并分别映射到左右轮。不要使用 `/dev/ttyUSB0`。

建议为左右轮建立固定 udev 别名，例如：

```text
/dev/serial/custom/left_motor
/dev/serial/custom/right_motor
```

然后在 YAML 中使用固定别名，而不是可能随插拔变化的 `/dev/ttyUSB1`、`/dev/ttyUSB2`。

### 方案 B：HDSC USB2CAN

如果实际电机通过 HDSC USB2CAN 控制，则不能使用当前 YZ Modbus `run_motor`。需要改为项目中 iWMC/SLCAN 相关驱动，并使用：

```text
/dev/serial/custom/wheels_usb2can
```

必须先根据电机和控制器实物型号确定使用方案 A 还是方案 B。

## 5. 检查 ROS2 启动文件

当前实际启动文件为：

```text
~/xline_ws/src/xline_bringup/launch/system_test.launch.py
```

它会启动：

- Foxglove Bridge
- 两轮电机驱动
- 北微 IMU
- LN150 驱动
- 速度仲裁节点
- 定位节点
- 路径规划节点
- 底盘控制节点

查找启动文件：

```bash
find ~/xline_ws/src -name "*.launch.py"
```

> 当前串口冲突尚未解决时，不要直接运行整套 `system_test.launch.py`，因为它会启动错误映射的电机驱动。

串口修复并完成架空测试准备后，才运行：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws/install/setup.bash
ros2 launch xline_bringup system_test.launch.py
```

## 6. 检查 ROS2 节点、话题和服务

另开一个 SSH 终端，加载 ROS2 环境后执行：

```bash
ros2 node list
ros2 topic list
ros2 service list
```

正式运行时至少应包含以下关键节点：

```text
/differential_wheels_driver
/cmd_vel_mux
/localization
/path_planner
/base_controller
```

主要话题和服务：

```text
/cmd_vel
/imu
/robot_pose
/reflector_position
/odom
/printer/quick_command
/ln_driver/command_srv
```

检查速度话题：

```bash
ros2 topic info /cmd_vel
```

`Subscription count` 应大于 `0`，但这仍不能证明电机串口已经连接成功，还要检查 ROS2 启动日志。

## 7. 将 App 后端复制到小车

Windows 项目后端位于：

```text
D:\xikao\test1\backend
```

复制到小车：

```powershell
scp -r D:\xikao\test1\backend qingz@192.168.0.100:~/xline_app_backend
```

后续只更新 Python 源码时，也可以重新复制 `backend\app` 目录。

## 8. 安装 FastAPI 后端依赖

在线安装：

```bash
cd ~/xline_app_backend
python3 -m pip install --user -r requirements.txt
```

如果 PyPI 出现 SSL 或 DNS 错误，可以尝试国内镜像：

```bash
python3 -m pip install --user -r requirements.txt \
  -i https://pypi.tuna.tsinghua.edu.cn/simple \
  --trusted-host pypi.tuna.tsinghua.edu.cn
```

如果小车无法解析任何互联网域名，应在联网电脑下载 ARM64/Python 3.10 离线依赖，再将 `wheelhouse` 复制到：

```text
~/xline_app_backend/wheelhouse
```

小车离线安装命令：

```bash
cd ~/xline_app_backend
python3 -m pip install --user --no-index --find-links wheelhouse \
  fastapi==0.116.1 \
  uvicorn==0.35.0 \
  pydantic==2.11.7 \
  websockets==15.0.1
```

验证依赖：

```bash
python3 -c "import fastapi,uvicorn,pydantic,websockets; print('依赖安装成功')"
```

`rclpy` 由 ROS2 Humble 提供，不通过 `pip` 安装。

## 9. 启动 FastAPI + ROS2 Bridge

### 前台启动（适合查看日志）

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws/install/setup.bash
cd ~/xline_app_backend
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

使用期间不要关闭该终端。

### 后台持续运行

需要在 SSH 退出后继续运行时，使用独立会话：

```bash
setsid -f bash -lc '
source /opt/ros/humble/setup.bash &&
source ~/xline_ws/install/setup.bash &&
cd ~/xline_app_backend &&
exec python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
' > ~/xline_backend.log 2>&1 < /dev/null
```

检查进程和端口：

```bash
pgrep -af "uvicorn app.main:app"
ss -lntp | grep 8000
tail -f ~/xline_backend.log
```

## 10. 检查后端真实状态

在电脑浏览器打开：

```text
http://192.168.0.100:8000/health
```

后端正常、但机器人节点尚未启动时，可能返回：

```json
{
  "ok": true,
  "status": {
    "ros_available": true,
    "bridge_mode": "rclpy",
    "online": true,
    "control_ready": false,
    "mission_nodes_ready": false,
    "battery": null,
    "linear_velocity": null,
    "robot_pose": {},
    "printer_status": "unknown"
  }
}
```

字段含义：

- `online=true`：FastAPI Bridge 正常。
- `ros_available=true`：后端能够加载 `rclpy`。
- `control_ready=true`：底盘驱动和速度仲裁节点已出现在 ROS2 图中。
- `mission_nodes_ready=true`：底盘、定位、规划和任务相关节点均已出现。
- `battery=null`：没有真实电量来源，App 应显示 `--`。
- `robot_pose={}`：没有收到定位数据，App 应显示“未定位”。
- `printer_status=unknown`：没有收到喷码机状态，App 应显示“未知”。

注意：节点存在仍不等于硬件串口成功。必须同时检查 ROS2 日志中是否存在“电机串口连接失败”。

## 11. 在 App 中连接小车

打开 App，进入：

```text
首页 -> 设备 -> 添加设备
```

填写：

```text
设备名称：XLine-Car-01
机器人 IP：192.168.0.100
端口：8000
ROS Domain：0
连接类型：FastAPI Backend
```

点击“连接测试”，再点击“保存并连接”。

连接成功后，顶部会显示“已连接”。此时 App 会根据真实后端数据决定功能是否可用：

- 无电量数据：显示 `--`。
- 无速度数据：显示 `--`。
- 无定位数据：显示“未定位”。
- 无喷码状态：显示“未知”。
- 无 `/robot_pose`：实时视图显示“等待定位数据”。
- 必需 ROS2 节点缺失：地图、控制和任务按钮保持禁用。

## 12. 首次安全测试

必须同时满足以下条件：

- 电机控制器型号已经确认。
- 左右轮串口映射正确且不占用 IMU。
- 小车已架空，车轮悬空。
- 周围无人且急停可用。
- ROS2 日志显示两个电机连接和初始化成功。
- `/health` 中 `control_ready=true`。

先只监听速度话题，不发送运动指令：

```bash
ros2 topic echo /cmd_vel
```

随后在 App 中：

1. 进入“首页 -> 控制”。
2. 将速度调到最低。
3. 先点击“停止”。
4. 短按一次“前进”。
5. 立即点击“停止”。
6. 检查左右轮方向、响应和 ROS2 日志。

如果任意车轮方向错误、持续运动或无法停止，立即使用硬件急停并关闭电机电源。

## 13. 常见问题

### App 显示连接失败

依次检查：

```powershell
ping 192.168.0.100
Test-NetConnection 192.168.0.100 -Port 8000
```

再打开：

```text
http://192.168.0.100:8000/health
```

### App 已连接但控制入口不可用

查看 `/health` 中：

```text
control_ready
mission_nodes_ready
missing_required_nodes
```

这通常说明网络和后端正常，但 ROS2 必需节点没有启动。

### 后端显示 simulated

启动后端前没有加载 ROS2 环境。重新执行：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws/install/setup.bash
```

### 8000 端口未监听

```bash
pgrep -af "uvicorn app.main:app"
cat ~/xline_backend.log
```

如果防火墙阻止访问：

```bash
sudo ufw allow 8000/tcp
```

### 电机串口连接失败

```bash
ls -l /dev/ttyUSB* /dev/ttyACM* 2>/dev/null
ls -l /dev/serial/custom 2>/dev/null
fuser -v /dev/ttyUSB0 /dev/ttyACM0 2>/dev/null
```

不要通过反复尝试 `/dev/ttyUSB0`、`/dev/ttyUSB1` 来猜设备。应根据 VID、PID、序列号和 udev 固定别名确认。

### ROS Domain 不一致

```bash
echo $ROS_DOMAIN_ID
```

App 默认使用 Domain `0`。小车 ROS2 节点与 FastAPI 后端必须使用相同的 Domain。

## 14. 当前项目的最简启动流程

1. 检查网络与 SSH。
2. 检查电机控制器和 USB 串口映射。
3. 确认没有串口冲突。
4. 启动 ROS2 节点并检查日志。
5. 启动 FastAPI Bridge。
6. 检查 `/health` 的真实状态字段。
7. 打开 App 并点击连接。
8. 确认控制与任务状态是否真正就绪。
9. 架空小车后进行最低速度测试。
