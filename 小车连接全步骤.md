# XLine 划线小车连接全步骤

## 1. 连接网络

1. 启动小车 Ubuntu 系统。
2. 将电脑、测试手机或 Android 模拟器与小车连接到同一个局域网。
3. 确认小车 IP 地址为 `192.168.0.100`。
4. 在 Windows PowerShell 中测试网络：

```powershell
ping 192.168.0.100
```

能够收到回复后再继续。如果超时，请检查小车 IP、Wi-Fi、路由器及防火墙。

## 2. SSH 登录小车

在 Windows PowerShell 中执行：

```powershell
ssh qingz@192.168.0.100
```

根据提示输入小车 SSH 密码。本文档不记录密码。

## 3. 加载 ROS2 环境

在小车 Ubuntu 终端执行：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws/install/setup.bash
```

检查 XLine 软件包：

```bash
ros2 pkg list | grep xline
```

检查节点、话题和服务：

```bash
ros2 node list
ros2 topic list
ros2 service list
```

项目使用的主要 ROS2 接口包括：

```text
/cmd_vel
/imu
/robot_pose
/reflector_position
/printer_status
/printer/quick_command
/ln_driver/command_srv
```

## 4. 启动小车 ROS2 节点

先查找小车已安装的启动文件：

```bash
find ~/xline_ws/install/xline_bringup -name "*.launch.py"
```

然后使用实际的启动文件：

```bash
ros2 launch xline_bringup <启动文件名称>
```

启动后再次运行 `ros2 node list` 和 `ros2 topic list`，确认底盘、定位、LN150 和喷码机等节点正常。

## 5. 将 App 后端复制到小车

后端源码位于：

```text
D:\xikao\test1\backend
```

在 Windows PowerShell 中执行：

```powershell
scp -r D:\xikao\test1\backend qingz@192.168.0.100:~/xline_app_backend
```

## 6. 安装后端依赖

重新 SSH 登录小车，然后执行：

```bash
cd ~/xline_app_backend
python3 -m pip install -r requirements.txt
```

如果系统没有 `pip`：

```bash
sudo apt update
sudo apt install python3-pip
```

`rclpy` 由 ROS2 Humble 提供，不需要通过 `pip` 安装。

## 7. 启动 FastAPI + ROS2 Bridge 后端

打开一个新的小车 Ubuntu 终端，执行：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws/install/setup.bash
cd ~/xline_app_backend
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

看到以下类似信息表示后端已启动：

```text
Uvicorn running on http://0.0.0.0:8000
```

使用 App 期间不要关闭该终端。

## 8. 检查后端状态

在电脑浏览器中打开：

```text
http://192.168.0.100:8000/health
```

正常返回数据中应包含：

```json
{
  "ok": true,
  "status": {
    "ros_available": true,
    "bridge_mode": "rclpy",
    "online": true
  }
}
```

如果 `bridge_mode` 显示为 `simulated`，说明启动后端前没有正确加载 ROS2 环境。重新执行第 7 步的所有命令。

## 9. 在 App 中配置小车

打开 App，进入：

```text
首页 -> 设备 -> 添加设备
```

填写以下信息：

```text
设备名称：XLine-Car-01
机器人 IP：192.168.0.100
端口：8000
ROS Domain：0
连接类型：FastAPI Backend
```

先点击“连接测试”，然后点击“保存并连接”。

连接成功后：

- 首页显示小车状态。
- 地图和控制入口自动解锁。
- 任务页可以执行划线任务。
- 连接状态显示为“已连接”。

## 10. 首次控制测试

> 测试前请将小车架空，或放在宽敞、无人的区域。

1. 进入“首页 -> 控制”。
2. 先点击“停止”。
3. 将速度调到最低。
4. 短按“前进”。
5. 再次点击“停止”。

在小车 Ubuntu 终端中检查速度指令：

```bash
ros2 topic echo /cmd_vel
```

如果能看到速度消息，说明 App、FastAPI 后端和 ROS2 已经连通。

## 11. 常见问题

### App 一直显示连接失败

依次检查：

```powershell
ping 192.168.0.100
```

然后打开：

```text
http://192.168.0.100:8000/health
```

### 8000 端口无法访问

在小车 Ubuntu 中执行：

```bash
sudo ufw allow 8000/tcp
```

### 后端显示模拟模式

关闭后端，重新加载 ROS2 环境，再启动后端：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws/install/setup.bash
```

### 指令发送成功但小车不动

检查 `/cmd_vel` 话题是否有订阅者：

```bash
ros2 topic info /cmd_vel
```

### ROS Domain 不一致

检查当前 Domain：

```bash
echo $ROS_DOMAIN_ID
```

App 默认使用 `0`。小车 ROS2 节点与后端必须使用相同的 `ROS_DOMAIN_ID`。

## 12. 每次启动的最简流程

1. 启动小车 ROS2 节点。
2. 加载 ROS2 环境。
3. 启动 `uvicorn` 后端。
4. 检查 `/health` 接口。
5. 打开 App 并点击“连接”。
6. 先低速测试，再执行正式任务。
