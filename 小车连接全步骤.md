# XLine 划线小车连接全步骤

本文档包含两种使用方式：

1. Android 模拟器连接本地虚拟小车，用于界面和功能演示。
2. App 连接真实 Ubuntu + ROS2 小车，用于设备联调。

> 安全说明：App 显示“已连接”只代表 WebSocket/FastAPI 通信成功，不等于底盘、电机、定位、避障和喷码机已经就绪。真实小车首次运动前必须架空车轮，并确保硬件急停可用。

## 一、快速判断应该使用哪套流程

| 使用场景 | App 设备地址 | 后端运行位置 |
| --- | --- | --- |
| Android 模拟器 Demo | `ws://10.0.2.2:8000` | Windows 电脑 |
| Windows 上的其他客户端 | `ws://127.0.0.1:8000` | Windows 电脑 |
| 真实小车 | `ws://192.168.0.100:8000` | 小车 Ubuntu |

`10.0.2.2` 是 Android 模拟器访问宿主 Windows 的专用地址，不能用于真实手机。

---

# 第一部分：连接虚拟 Demo 小车

## 1. 启动 Android 模拟器

在 Android Studio Device Manager 中启动名为 `xikao` 的模拟器。

检查设备：

```powershell
D:\ai\android\platform-tools\adb.exe devices
```

应看到：

```text
emulator-5554    device
```

## 2. 启动 Demo 后端

打开一个新的 PowerShell 窗口并执行：

```powershell
cd D:\xikao\test1\backend
D:\python\3.13\python.exe run_demo_backend.py
```

出现以下信息表示启动成功：

```text
Uvicorn running on http://0.0.0.0:8000
```

使用期间不要关闭该窗口。

### 后台启动方式

```powershell
Start-Process `
  -FilePath "D:\python\3.13\python.exe" `
  -ArgumentList "run_demo_backend.py" `
  -WorkingDirectory "D:\xikao\test1\backend" `
  -WindowStyle Hidden
```

## 3. 验证 Demo 后端

```powershell
Invoke-WebRequest http://127.0.0.1:8000/health
```

返回状态码 `200` 表示服务正常。

也可在浏览器打开：

```text
http://127.0.0.1:8000/health
```

## 4. 在 App 中连接

Debug APK 默认设备应为：

```text
设备名称：XLine-Demo-01
IP：10.0.2.2
端口：8000
WebSocket：ws://10.0.2.2:8000
```

打开 App，点击“重新连接”。顶部显示“已连接”后，即可查看虚拟地图、遥测、任务和画图工具。

## 5. Demo 再次连接失败

依次检查：

```powershell
D:\ai\android\platform-tools\adb.exe devices
Invoke-WebRequest http://127.0.0.1:8000/health
Test-NetConnection 127.0.0.1 -Port 8000
```

常见原因：

- 模拟器重启后，Demo 后端没有重新启动。
- 启动后端的 PowerShell 窗口被关闭。
- 8000 端口被其他程序占用。
- App 设备地址被改成了真实小车地址。

---

# 第二部分：连接真实小车

## 1. 当前环境

```text
小车系统：Ubuntu 22.04
ROS2：Humble
小车用户：qingz
小车 IP：192.168.0.100
ROS2 工作空间：~/xline_ws2
App 后端：~/xline_app_backend
后端端口：8000
ROS Domain ID：0
```

## 2. 连接同一局域网

电脑、手机和小车必须处于同一局域网。

Windows 检查：

```powershell
ping 192.168.0.100
Test-NetConnection 192.168.0.100 -Port 22
```

`TcpTestSucceeded` 应为 `True`。

## 3. SSH 登录小车

```powershell
ssh qingz@192.168.0.100
```

建议准备三个 SSH 终端：

- 终端 A：启动 ROS2。
- 终端 B：启动 FastAPI Bridge。
- 终端 C：执行节点、话题和日志检查。

不要把 SSH 密码写入项目文件或本文档。

## 4. 加载 ROS2 环境

每个新的小车终端都要执行：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws2/install/setup.bash
export ROS_DOMAIN_ID=0
```

验证：

```bash
echo $ROS_DISTRO
echo $ROS_DOMAIN_ID
ros2 pkg list | grep xline
```

## 5. 检查 USB 和固定设备映射

```bash
lsusb
ls -l /dev/ttyUSB* /dev/ttyACM* 2>/dev/null
ls -l /dev/serial/custom 2>/dev/null
```

已确认的设备映射：

| 硬件 | 原始设备 | 固定别名 |
| --- | --- | --- |
| 北微 IMU | `/dev/ttyUSB0` | `/dev/serial/custom/imu` |
| HDSC USB2CAN | `/dev/ttyACM0` | `/dev/serial/custom/wheels_usb2can` |

底盘按当前 `xline_ws2` 配置默认使用香橙派原生 SocketCAN，接口为 `can0`。USB2CAN 仅作为兼容方案保留；不要把 `/dev/ttyUSB0` 分配给电机，它属于 IMU。

检查当前 CAN 接口：

```bash
ip -details link show can0
cat /sys/class/net/can0/operstate
```

正常运行时接口应存在并处于 `UP` 状态。

检查占用：

```bash
fuser -v /dev/ttyUSB0 /dev/ttyACM0 2>/dev/null
```

## 6. 构建 ROS2 工作空间

源代码变化后执行：

```bash
cd ~/xline_ws2
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
```

构建失败时不要继续启动底盘。

## 7. 启动 ROS2 系统

确认小车车轮架空、周围无人且急停可用后执行：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws2/install/setup.bash
export ROS_DOMAIN_ID=0
ros2 launch xline_bringup system_test.launch.py enable_hardware:=true
```

另开终端检查：

```bash
source /opt/ros/humble/setup.bash
source ~/xline_ws2/install/setup.bash
ros2 node list
ros2 topic list
ros2 service list
```

重点确认：

```text
/cmd_vel_mux
/localization
/path_planner
/base_controller
/robot_pose
/imu
/odom
/tablet_cmd_vel
/plan_path
/execute_plan
/execution/pause
/execution/resume
```

节点出现不代表硬件一定正常，还要检查启动日志中是否存在 SocketCAN、电机或串口错误。

## 8. 部署 App 后端

Windows 项目后端位于：

```text
D:\xikao\test1\backend
```

首次复制：

```powershell
scp -r D:\xikao\test1\backend qingz@192.168.0.100:~/xline_app_backend
```

安装依赖：

```bash
cd ~/xline_app_backend
python3 -m pip install --user -r requirements.txt
```

如果 PyPI SSL 连接失败：

```bash
python3 -m pip install --user -r requirements.txt \
  -i https://pypi.tuna.tsinghua.edu.cn/simple \
  --trusted-host pypi.tuna.tsinghua.edu.cn
```

`rclpy` 由 ROS2 Humble 提供，不通过 pip 安装。

## 9. 启动真实小车后端

当前小车使用 systemd 管理后端，服务名为 `xline-app-backend`：

```bash
sudo systemctl enable --now xline-app-backend
sudo systemctl restart xline-app-backend
systemctl --no-pager --full status xline-app-backend
```

查看实时日志：

```bash
journalctl -u xline-app-backend -f
```

检查端口和进程：

```bash
pgrep -af "uvicorn app.main:app"
ss -lntp | grep 8000
```

服务通过 `~/xline_app_backend/run_robot_backend.sh` 自动加载 ROS2 Humble 和
`~/xline_ws2/install/setup.bash`。不要同时手工启动第二个 Uvicorn，否则会占用同一个端口。

## 10. 验证真实后端

在 Windows 浏览器打开：

```text
http://192.168.0.100:8000/health
```

或执行：

```powershell
Test-NetConnection 192.168.0.100 -Port 8000
Invoke-WebRequest http://192.168.0.100:8000/health
```

重点检查返回状态中的：

```text
online
ros_available
control_ready
mission_nodes_ready
drive_device_connected
motor_driver_ready
missing_required_nodes
```

只有 `control_ready=true` 才能认为 App 已具备底盘控制条件。

## 11. 在 App 中添加真实小车

进入：

```text
首页 -> 设备 -> 添加设备
```

填写：

```text
设备名称：XLine-Car-01
机器人 IP：192.168.0.100
端口：8000
ROS Domain ID：0
连接类型：FastAPI Backend
```

先执行“连接测试”，成功后再保存并连接。

## 12. 首次安全联调

首次运动测试必须满足：

- 小车车轮架空。
- 周围无人和障碍物。
- 硬件急停可立即触达。
- SocketCAN `can0` 已识别并处于 `UP`。
- 电机节点无错误。
- `/health` 中 `control_ready=true`。
- App 连接链路和底盘驱动均显示正常。

先监听命令：

```bash
ros2 topic echo /tablet_cmd_vel
```

然后在 App 中：

1. 进入“首页 -> 控制”。
2. 将线速度和角速度调到最低。
3. 先点击停止。
4. 短按一次前进。
5. 立即点击停止。
6. 检查左右轮方向、反馈和 ROS2 日志。
7. 测试 App 急停和硬件急停。

出现方向错误、持续运动、无反馈或无法停车时，立即使用硬件急停并断开电机电源。

---

# 第三部分：AI 助手与 JSON 图纸

## 1. AI 配置保存位置

真实小车的 AI 配置保存在：

```text
/home/qingz/.config/xline-agent.json
```

不同服务商的 API Key 可以同时保存，但同一时间只启用当前选择的服务商和模型。

- API Key 留空后保存：保留当前服务商的旧 Key。
- 输入新 Key 后保存：覆盖当前服务商的旧 Key。
- 点击删除按钮：只删除当前服务商的 Key。
- Demo 后端与真实小车后端的 AI 配置相互独立。

DeepSeek 使用标准模型名：

```text
deepseek-chat
deepseek-reasoner
```

## 2. AI 创建图纸

例如输入：

```text
规划一个 5x3 米矩形
```

助手会先返回确认操作。确认后仅生成 JSON 图纸，不会自动启动或移动小车。生成后应进入任务页检查图纸预览，再由现场人员确认环境和急停后启动任务。

## 3. JSON 图纸工具

任务页支持导入 CAD JSON。坐标单位按规划器配置使用毫米，后端通过 `/plan_path` 调用路径规划器，再通过 `/execute_plan` 发送 `plan_json`。

导入图纸后必须检查预览、尺寸、定位有效状态和喷码机需求，不能把“JSON 校验通过”等同于“可以安全执行”。

---

# 第四部分：常见故障排查

## 1. App 显示连接失败

Demo 模式：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/health
```

真实小车：

```powershell
ping 192.168.0.100
Test-NetConnection 192.168.0.100 -Port 8000
Invoke-WebRequest http://192.168.0.100:8000/health
```

## 2. 已连接但地图或控制不可用

这通常表示 FastAPI 正常，但 ROS2 节点或硬件未就绪。检查：

```bash
ros2 node list
ros2 topic list
journalctl -u xline-app-backend -n 100 --no-pager
```

同时查看 `/health` 中的 `missing_required_nodes`。

## 3. 8000 端口未监听

Windows Demo：重新运行 `run_demo_backend.py`。

小车 Ubuntu：

```bash
pgrep -af uvicorn
ss -lntp | grep 8000
systemctl --no-pager --full status xline-app-backend
journalctl -u xline-app-backend -n 100 --no-pager
```

## 4. SocketCAN 或电机未就绪

```bash
ip -details link show can0
cat /sys/class/net/can0/operstate
ip -statistics link show can0
journalctl -u xline-app-backend -n 100 --no-pager
```

如果明确切换到了 USB2CAN 兼容模式，再检查 `/dev/serial/by-id/` 和对应 udev 固定别名；不要通过反复更换设备路径猜测硬件。

## 5. ROS Domain 不一致

```bash
echo $ROS_DOMAIN_ID
```

App、ROS2 节点和 FastAPI 后端统一使用 Domain `0`。

## 6. 防火墙阻止 8000 端口

小车 Ubuntu：

```bash
sudo ufw allow 8000/tcp
sudo ufw status
```

---

# 最简启动清单

## 模拟器 Demo

1. 启动 `xikao` 模拟器。
2. 在 Windows 启动 `backend/run_demo_backend.py`。
3. 检查 `http://127.0.0.1:8000/health`。
4. App 选择 `XLine-Demo-01`。
5. 点击“重新连接”。

## 真实小车

1. 检查网络、SSH、SocketCAN `can0` 和急停。
2. 加载 ROS2 环境并使用 `enable_hardware:=true` 启动 `system_test.launch.py`。
3. 检查节点、话题和硬件日志。
4. 在小车启动 FastAPI + rclpy 后端。
5. 检查 `http://192.168.0.100:8000/health`。
6. App 选择 `XLine-Car-01` 并连接。
7. 确认底盘、定位、避障和任务状态。
8. 架空车轮后进行最低速度测试。
